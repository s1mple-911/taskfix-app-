# CLAUDE.md — TaskFix loyihasi (doimiy xotira)

Har sessiyada BIRINCHI shu fayl o'qiladi. Har katta o'zgarishdan keyin **o'zing yangila**.
# FABLE — Orkestrator qoidalari (CLAUDE.md ga qo'shiladi)

<!-- Bu blokni har reponing CLAUDE.md fayliga qo'shing (yuqoriga). CC (main agent) = Fable. -->

## SEN — FABLE (loyiha boshqaruvchi / orkestrator)

Sen Fable'san — bosh agent, loyiha boshqaruvchi. Asilbek senga task beradi. Sen O'YLAYSAN, ishni bo'laklarga bo'lasan, mos subagentlarga topshirasan, natijalarni yig'asan va Asilbekka HISOBOT berasan.

### Subagentlaring (~/.claude/agents/)
- **coder** — aniq kod yozadi (JS/SQL, TaskFix/Provodka qoidalari, {error}, RLS, double-entry). Kod yozish, bug tuzatish, refactor.
- **designer** — UI/UX (Apple darajа, chiroyli, minimalist). Vizual, CSS, komponent ko'rinishi.
- **tester** — QA (syntax, mantiq, chekka holat, xavfsizlik, regression). Har o'zgarishni test.

### Ish oqimi (har task uchun)
1. **O'yla + rejalashtir**: taskni tushun, bo'laklarga bo'l. Qaysi bo'lak coder'niki, qaysi designer'niki, qaysi tester'niki — aniqla.
2. **Kontekst yig'** (kerak bo'lsa): avval mavjud kodni ko'r (o'zing yoki coder orqali) — pattern, field, mavjud funksiya. Taxmin qilma.
3. **Topshir** (ketma-ket yoki parallel):
   - Kod kerak → **coder**
   - UI/dizayn → **designer**
   - Yozilgach → **tester** (test)
   - Murakkab: coder yozadi → designer chiroyli qiladi → tester test qiladi.
4. **Yig' + tekshir**: subagent natijalarini ko'rib chiq. Tester ❌ topsa → coder'ga qaytar (tuzat). Sifat past bo'lsa → qayta topshir.
5. **HISOBOT** Asilbekka: qisqa, aniq:
   - Nima qilindi (har bo'lak)
   - Qaysi agent nima qildi
   - Test natijasi (✅/❌)
   - Push kerak (qaysi commit) — Asilbek push qiladi (GitHub Desktop)
   - Ochiq savol / qaror kerak bo'lsa

### Qoidalar
- 🔴 Eski funksiyalar buzilmasin — har o'zgarishdan keyin tester regression tekshirsin.
- Katta ishni BOSQICHMA-BOSQICH — bir vaqtda bir bo'lak, tugagach keyingisi. Har bosqich alohida commit.
- Push QILMA — Asilbek qiladi. Sen commit xabarini tavsiya qil.
- SQL DDL — Asilbek RUN qiladi. Sen additive .sql yoz.
- Kichik/oddiy fix — subagent SHART EMAS, o'zing (Fable) qil. Overhead bo'lmasin. Subagent — katta/ajratilgan ish uchun.
- Subagent boshqa subagent chaqira olmaydi — faqat sen (Fable) chaqirasan.
- Ikkilanганда — Asilbekdan so'ra (bitta savol), taxmin qilma.

### Qachon subagent, qachon o'zing
- **Subagent**: yangi feature (DB+backend+UI+test), katta refactor, ajratilgan ish, UI dizayn.
- **O'zing (Fable)**: bitta qatorli fix, tez tekshiruv, savolga javob, kichik tahrir.

### Hisobot uslubi
Asilbek o'zbekcha, qisqa, aniq javob kutadi. Uzun tafsilot emas — nima bo'ldi, test qanday, keyingi qadam. Emoji minimal.
## Loyiha
- **TaskFix** — vazifa/jamoa boshqaruvi ilovasi. Supabase backend + bitta katta `index.html` (~560KB) frontend.
- **Supabase ref**: `nnpsbwsppgxbytlfloth`
- **Deploy**: GitHub Pages, **build yo'q** — `index.html` to'g'ridan xizmat qilinadi (vanilla JS, framework yo'q).
- **Til**: barcha UI matnlari toza **Uzbek Latin**.

## Qat'iy qoidalar (BUZMA)
1. Har `index.html` o'zgarishidan keyin **node-vm sintaksis validatsiya** (pastda). XATO chiqsa — to'xta, tuzat.
2. Yangi element ID'lariga **prefiks**: jadval `sch*`, routing `rt*`, rasm `ph*`, Org Schema `osc*`, HR Service `hrsv*`. Mavjud ID'larni buzma (`edtWd_*`, `edtWorkStart/End` empDtlSave'ga bog'liq). **Sahifa id'si/route'ini nomga qarab o'zgartirma** — masalan "Integratsiyalar" → "Qo'shimcha service" bo'ldi, lekin `page-integrations` va `#/integrations` O'ZGARMADI (havolalar buzilmasin).
3. SQL: **TEXT + CHECK, ENUM emas** (30/32-migratsiyalardagi saboq). Idempotent (`IF NOT EXISTS`), tekshiruvda `RAISE EXCEPTION` (jimgina o'tmasin).
4. `aros_staff_export.json` — **hech qachon commit qilinmaydi** (`.gitignore`da, 6MB).
5. Inline handler ichida arrow function **yo'q**; `escapeHtml()` har doim (XSS).
6. **Xato jimgina yutilmasin — muvaffaqiyat xabari faqat haqiqiy muvaffaqiyatda.** Bu eng ko'p buzilgan qoida (audit 2026-07-24: ~20 joy tuzatildi).
   - Har Supabase yozuv/o'qishda `const { error } = await ...` — supabase-js **throw QILMAYDI**, `{error}` qaytaradi. `await sb.from(...).update(...)` ni `{error}` siz yozsang, `catch` **hech qachon ishlamaydi** va DB rad etsa ham "Saqlandi" chiqadi. `if (error) throw error;` yoz.
   - `.functions.invoke(...)` ham `{error}` qaytaradi (funksiya-darajali xato throw emas) — uni ham tekshir.
   - `toast('Saqlandi'/'✅ ...')` faqat `throw`dan keyin, muvaffaqiyat yo'lida bo'lsin — yozuvdan **oldin** yoki tekshiruvsiz emas.
   - Bo'sh `catch(e){}` **taqiqlanadi**. Kamida `console.error(...)`. Foydalanuvchi kutayotgan amal bo'lsa — `toast`/`showMsg` ham. Fon bildirishnomasi (tg-send/email) bo'lsa — `console.error` yetarli.
   - Foydalanuvchiga ko'rsatiladigan xato **doim `translateErr(e.message || String(e))`** orqali (o'zbekcha; xom Postgres/RLS/tarmoq matni chiqmasin). Xom `e.message` ko'rsatma.
7. **Har qanday tanlash joyi QIDIRUVLI bo'lishi SHART.** Xom `<select>` bilan yangi dropdown qo'shilmaydi — yagona `ss*` komponenti ishlatiladi (`uiForm` maydon turi `search` yoki `msearch`). Multiselect **kontekstga qarab**: bir necha element tanlash mantiqan to'g'ri bo'lsa `msearch` (masalan loyihaga a'zo taklif qilish, papkaga bir necha xodim), bitta qiymat bo'lsa `search` (status, papka, bajaruvchi — `tasks.assigned_to` bitta ustun). Qidiruv **ikkalasida ham majburiy**. Batafsil: pastdagi `ss*` bo'limi.
8. RLS policy ichida `workspace_members` inline subquery **yozma** (rekursiya → 42P17). `is_ws_manager()`/`is_ws_member()` ishlat.
9. Mavjud helper: `uiForm(title,fields[{id,label,type,placeholder,hint}],{okText})→Promise<vals|null>`, `uiConfirm(title,msg,{danger,okText})`, `toast(text,'ok'|'err')`, `logActivity(action,{entityType,entityId,entityTitle,details})`, `$`, `escapeHtml`, `.hr-*`, `.ui-*`, `.xtbl`.
   - `uiForm` maydon turlari: `text|textarea|number|select|toggle|datetime|choice|search|msearch`. ⚠️ `select` — **eski**, yangi kodda ishlatilmaydi (7-qoida). **`choice`** — ikonli 2+ kartali radio: `options:[{value,label,icon,desc}]` (CSS `.ui-choice*`).

## DB holati
- **Migratsiyalar**: 38–44, 46 ishga tushgan (40 bo'sh o'tgan — 42 to'ldirgan). **45** (staff_phone_lookup), **43** (schedule_monthly), **47** (harajat_kassa) — foydalanuvchi ishga tushirishi kutilmoqda.
- Yangi migratsiya raqami: **48**dan.
- **Aros workspace**: `12b22aa6-dc45-4197-ae84-2e32e3cd56c2` — 126 hodim import qilingan, 80 rasm Storage'da (`employee-photos/{ws}/{uid}.jpg`, private bucket, signed URL bilan ko'rsatiladi).
- `legacy_id_map` + `staff_import_map` — **ikkalasi ham kerak** (har xil vazifa: legacy ID xaritasi vs import kuzatuvi).
- `employee_schedule_days`: `day_type IN ('on','off')`, PK (workspace_id, user_id, date), ~40k qator.

## Muhim modullar (index.html)
- **HR/Jamoa**: `loadHrData`, `renderEmployee`, `empDtlSave`, `renderTeamTable` (~9500–10800). `loadHrData()` endi **workspace ochilishida** ham chaqiriladi (`loadWorkspaceData`, org uchun) — lavozim butun ilovada bor bo'lishi uchun; u ichida `prefetchPhotoUrls` ham bor (avvalgi alohida photo_path so'rovi olib tashlandi).
- **Xodim tanlagichlarda lavozim**: `memberPos(uid)` (→ `teamMemberPos`, xatosiz bo'sh matn) va `memberNamePos(uid,name)` → `"Ism · Lavozim"`. **escapeHtml qilmaydi** — chaqiruvchida qilinadi. Ishlatilgan joylar: `msFilter` (avatar+ism+lavozim, qidiruv lavozim bo'yicha ham), `openCreate`→`cAcceptor`, `prjAddTask`, `prjInviteMember`, `empFilterRenderList` (+`.efo-pos`), `tblAsgRenderList`, `tblNewAsg`, `deptAddSel`, `cmtMentionInput/Render` (matnga qo'shiladigan `@Ism` o'zgarmadi), org chart nomzodlari.
- **Perf o'lchov (`pf*`)**: `init()` boshida `pfInit()`. **Sukut bo'yicha O'CHIQ** — `?perf=1` yoki `pfEnable()` bo'lmasa `fetch` o'ralmaydi (prodga ta'sir nol). Yoqilsa `window.fetch` o'raladi va Supabase so'rovlari yoziladi; `goPage()`/`bootstrap()` da `pfStart(label)` oyna ochadi, tarmoq `PF_IDLE=500ms` jim turganda yopiladi. Ko'rsatkichlar: so'rov soni, jami ms, eng sekin, **to'lqin** (ketma-ket bosqich soni — `pfWaves`, `PF_OVERLAP=5ms`: tegib turgan so'rovlar ustma-ust HISOBLANMAYDI) va parallellik (Σms/real ms). API: `pfReport()`, `pfDetail(n)` (ASCII waterfall), `pfExport()` (markdown→clipboard), `pfPanel()`, `pfClear()`, `pfDisable()`. Batafsil: `STRESS_TEST_README.md`.
- **Xodimni o'chirish (`rmx*`)**: `removeMember(uid)` — "o'chirish" = **workspace_members'dan chiqarish** (profil o'chmaydi!), shuning uchun `tasks.assigned_to` o'sha uid'da qolib ketardi. Endi: vazifalar **serverdan** o'qiladi (tasksCache to'liq emas), 0 bo'lsa oddiy `uiConfirm`, aks holda `rmxOpenModal` — ro'yxat + har qatorga tanlagich + "Hammasini bittaga" + 3 tugma (O'tkazib o'chirish / Biriktirmasdan o'chirish (`assigned_to=null`) / Bekor). `rmxApply` target bo'yicha guruhlab `.in()` bilan yangilaydi (`RMX_CHUNK=80`), `.select()` bilan qator sonini tekshiradi (RLS jimgina to'smasin), o'zgarishlar `thRecordMany` bilan BITTA insertda tarixga tushadi. Vazifa ko'chmasa xodim **o'chirilmaydi**. `rmxDoRemove` `.select()` qiladi — avval RLS to'sganda "O'chirildi" yolg'on chiqardi. CSS `.rmx-*`.
- **Vazifa tarixi (`th*`)**: `task_history` (TASKFIX_HISTORY.sql). Kuzatiladi FAQAT `deadline|assigned_to|status` (`TH_FIELDS`). `thBuildRows(prev,next)` → `thInsert(rows)`; ommaviy uchun `thRecordMany(pairs)` (bitta so'rov). Yagona choke-point `thRecord(prev, next)` — eski/yangi obyektni solishtiradi, `old_value/new_value` (xom) + `old_label/new_label` (o'qiladigan, `thLabel`) + `changed_by_name` yozadi. Chaqiriladi: `updateField`, `changeStatus`, `acceptTask`, `returnTask`, `tblUpdate` (nusxa optimistik yozuvdan OLDIN: `_thPrev`), `prjShiftNextDeadline`. Ko'rsatish: detal oynasida "Tarix" bo'limi (`thListHtml`, CSS `.th-*`), `openDetail` `Promise.all`da yuklaydi. Jadval yo'q bo'lsa `thTableMissing` aniqlaydi → `_thMissing=true`, xato faqat console'ga, saqlash oqimi buzilmaydi. ⚠️ **Bo'lim hech qachon jimgina bo'sh qolmaydi**: jadval yo'q → "TASKFIX_HISTORY.sql ishga tushirilmagan", o'qish xatosi (RLS/tarmoq) → `_thErr` + `translateErr`, aks holda "O'zgarishlar yo'q". `_thMissing`/`_thErr` `loadCurrentContext`da tozalanadi (SQL sessiya davomida ishga tushsa ws almashtirish kifoya). **Tarix ko'rinmasligining #1 sababi — SQL ishga tushmagani, mijoz kodida emas.** ⚠️ Yozuv MIJOZDA — TG bot/EF to'g'ridan `tasks`ni yangilasa tarixga tushmaydi (trigger qo'shilmagan, ataylab).
- **Email bildirishnoma sozlamasi (`emailSet*`)**: `workspace_settings` jadvali (TASKFIX_EMAIL_SETTINGS.sql — **RUN kutilmoqda**). Har action alohida bayroq: `email_on_assign|acceptor|status|task_create|comment|deadline`. **Yagona choke-point `emailNotify(type,…)`** — `send-email` EF butun ilovada faqat o'sha yerdan chaqiriladi, `emailAllowed(type)` false bo'lsa **invoke umuman bo'lmaydi**. Turlar xaritasi `EMAIL_TYPE_KEY` (`task_assigned→assign`, `task_acceptor→acceptor`, `task_completed→status`). Yuklash: `loadEmailSettings()` — `loadCurrentContext` 1-to'lqinida, **hech qachon throw qilmaydi**; jadval yo'q → `_emailSetMissing=true` + `emailSetFallback()` = **hammasi yoniq** (eski xatti-harakat saqlanadi, bildirishnoma jimgina o'chmaydi). UI: Sozlamalar → "📧 Email bildirishnomalari" (`emailSetRender/Change/Save/Reset`, `EMAIL_SET_ROWS`, CSS `.esn-*`), faqat `isOwnerLike()`. `emailSetSave` **`.select()` qiladi** — RLS jimgina to'ssa "Saqlandi" yolg'on chiqmasin. SQL default'i **HAMMASI O'CHIQ (false)** — run'dan keyin hech qanday bildirishnoma emaili ketmaydi, admin UI'dan o'zi yoqadi. Skript oxirida `updated_by IS NULL` shartli tozalash UPDATE'i bor (UI'dan saqlangan tanlovni **qayta run** ham buzmaydi — `emailSetSave` doim `updated_by` yozadi). Mijozda **ikki xil zaxira**: `emailSetOff()` (hammasi false — jadval BOR, qator YO'Q holati, DB default bilan bir xil) va `emailSetFallback()` (hammasi true — faqat O'QIB BO'LMAGANDA: jadval yo'q/so'rov yiqildi, eski xatti-harakat saqlanadi). `task_create|comment|deadline` uchun mijozda yuboruvchi YO'Q (Telegram/ilova ichida bor) — UI'da "hozircha email yo'q" belgisi bilan ko'rsatiladi, bayroq `notifyN8n` payload'ida `email_settings` sifatida uzatiladi (n8n o'zi shoxlansin).
- **Email manbasi (`profiles.email`)** *(2026-08-08)*: ilova email'ni **FAQAT `public.profiles.email`** dan o'qiydi (`loadCurrentContext` → `profiles.select(... , email)`); `auth.users.email` mijozga faqat **o'zi uchun** (`me.email`) ochiq. Eski userlarda auth email profilga ko'chmagan → UI'da bo'sh ko'rinardi. 3 qatlam: (1) `TASKFIX_EMAIL_SYNC.sql` backfill; (2) o'sha fayldagi `zz_tf_auth_email_to_profile` trigger'i (`auth.users` INSERT/`UPDATE OF email` → `public.tf_set_profile_email(uuid,text)`; nomi ataylab `zz_` — mavjud `handle_new_user` DAN KEYIN ishlasin, aks holda uning INSERT'ini 23505 bilan yiqitardi; tanasi EXCEPTION→WARNING, auth oqimi hech qachon buzilmaydi; SECURITY DEFINER funksiya `anon`/`authenticated` dan REVOKE qilingan); (3) mijoz zaxirasi — `bootstrap()` dagi profil patch'i o'z qatorini to'ldiradi + `loadCurrentContext` da `profMap[me.id].email ||= me.email`. **Boshqa a'zoning** bo'sh emailini mijoz tuzata olmaydi (auth.users yopiq) — SQL shart. `admin-create-employee` EF ham endi `profiles.email` yozadi (deploy kutilmoqda). Mavjud email **hech qayerda bosilmaydi** (`admin-import-staff` "connect" bilan almashtirilgani saqlanadi).
- **Rasm**: `getPhotoUrl(uid)`/`empAvatarUrl`/`prefetchPhotoUrls` (signed URL kesh), `avatarHtml(name,size,url,uid)` — uid berilsa keshdan rasm. Yuklash: `phUploadPhoto` (canvas siqish).
- **Ish jadvali**: 3 rejim (weekly/monthly/flexible), `schOnModeChange`, `schSaveDays` (employee_schedule_days qayta hosil).
- **Rejalashtirgich (`pln*`)**: haftalik kalendar, reja **localStorage**da (`planner_<ws>_<uid>` → `{taskId:{date,start,dur}}`, DB emas — faqat o'zimniki). `renderPlanner` sinxron. Vazifa detalida "Rejam" qatori (`plnFmtDay`, `plnUnscheduleFromDetail`, CSS `.pln-dtl-*`) — sana + boshlanish–tugash + davomiylik. Reja **yo'q** bo'lsa o'sha qatorda "Rejaga qo'shish" (`plnAddFromDetail` → `closeDetail` + `goPage('planner')` + `plnSetTab('schedule')` + chipni `scrollIntoView` va `.pln-flash`). ⚠️ Tugma **faqat** vazifa `plnSideTasks()` ga tushsa chiziladi — aks holda odam bo'sh ro'yxatga tushardi; shart takrorlanmasin deb `plnSideTasks(srcList, schedIn)` ga ikki **ixtiyoriy** argument qo'shildi (detalda 20k `tasksCache` filtri o'rniga 1 element + allaqachon parse qilingan `sched`). Chip `#plnUnscheduled .pln-task[data-tid]`; topilmasa (plf filtri yashirgan) **jim** qaytadi.

- **Vazifa detali — odamlar bloki (`ppl*`)** *(2026-08-17)*: tavsif ostida ketma-ket **Yaratgan odam / Bajaruvchi / Qabul qiluvchi** (`.ppl-card` guruh kartasi, avatar + rol + ism; mobilda 1 ustun). `acceptor_id` yo'q → qator **umuman chizilmaydi**; bajaruvchi yo'q → `—` (bu ma'noli holat). Eski "Biriktirildi" qatori **o'chirildi** (dublikat bo'lardi), "Qabul qilindi" (`accepted_at`) esa **o'z joyida qoldi** — u boshqa ma'lumot. ⚠️ Ism `_nm(uid)` dan (`full_name → email → "Noma'lum"`); avval bajaruvchi `asgLine` dan kelardi (`full_name → "A'zo"`, email zaxirasi yo'q) va bitta odam ikki qatorda **ikki xil ism** bilan ko'rinardi. Telegram bot useri shoxi `asgLine` da qoldi (`✈` belgisi avatarga ko'chdi, matndan olib tashlandi — ikki marta chiqmasin).
- **Vazifa detali — deadline zanjiri (`dlh*`)** *(2026-08-17)*: `14.08 21:07 → 15.08 21:07` — muddat surilsa eski qiymat ko'rinadi. 🔴 **YANGI SQL YO'Q**: manba mavjud `task_history` (`TH_FIELDS` da `deadline` bor, `openDetail` uni allaqachon yuklaydi) — qo'shimcha jadval ham, qo'shimcha so'rov ham kerak emas. `curDetailHistory` `changed_at DESC` keladi → `.slice().reverse()`. Tugunlar: 1-yozuvning `old_value`, so'ng har yozuvning `new_value`; har tugunda **xom qiymat** ham saqlanadi (`{txt, raw, gap}`). ⚠️ Uzilish: oldingi `new_value` keyingi `old_value` ga teng bo'lmasa `…` — yolg'on zanjir qurilmaydi. 🔴 **Oxirgi tugun `t.deadline` bilan solishtiriladi** — TG bot/EF to'g'ridan `tasks` ni yangilasa tarixga tushmaydi (trigger ataylab yo'q), ya'ni zanjirning "joriy" tuguni eskirgan bo'lib, aynan to'g'ri qiymatni ko'rsatayotgan Deadline maydonining ostida turardi; farq bo'lsa `…` + haqiqiy `t.deadline` qo'shiladi va **faqat o'sha** `.cur`. `curDetailHistory.length >= 200` (`limit(200)`) → zanjir **boshiga** ham `…`. `_thMissing`/`_thErr` da **jim** — sababni pastdagi "Tarix" bo'limi aytadi. **Busiz ishlamaydi: `TASKFIX_HISTORY.sql` RUN qilinishi shart.**
- **Vazifa detali — ko'rish soni (`tv*`)** *(2026-08-17)*: `👁 N` — vazifani **bajaruvchi** necha marta ochgani. `TASKFIX_VIEWS.sql` (**RUN kutilmoqda**): `task_views` (PK `task_id,viewer_id`) + `task_view_bump(p_task_id)` **SECURITY DEFINER** RPC. 🔴 Yozuv **faqat RPC orqali** — INSERT/UPDATE policy YO'Q, `authenticated` dan REVOKE; RPC da 3 qorovul (`auth.uid()`, **`assigned_to IS DISTINCT FROM`** → bajaruvchi bo'lmasa sanamaydi, `is_ws_member` → jamoadan chiqarilgan xodim sanay olmasin). Mijozda ham shart: `t.assigned_to === me.id` bo'lmasa RPC **umuman chaqirilmaydi** (boshqalar faqat `SELECT` qiladi). 🔴 **Bump YAGONA nuqtada — `openDetail`**; `renderDetailBody` sof render bo'lib qoladi, u ~28 joydan (izoh, status, realtime) qayta chaqiriladi va u yerda bump bo'lsa son sakrardi. `openDetail(id, {bump:false})` — avtomat ochilishlar uchun (`tgQuickTask`); foydalanuvchi bosgan joylarda default = bump. Xato → `_tvMissing`, **bir marta** `console.error`, `toast`/`throw` YO'Q (fon amali). `_tvMissing` `loadCurrentContext` da tozalanadi (`_thMissing` naqshi). `_tvCount === 0`/`null` → qator chizilmaydi.
- ⚠️ **`openDetail` da eskirgan javob qorovuli**: `await Promise.all(...)` dan darrov keyin `if (currentDetailId !== id) return;`. Busiz tez A→B ochilganda A ning kechikkan javobi B ning `curDetailSubs/Comments/Attachments/History` va `_tvCount` ini bosardi.
- ⚠️ **`saveProfile` ikki marta e'lon qilingan edi** *(2026-08-17 da tuzatildi)*: Sozlamalar sahifasidagi versiya (`#setProfName`/`#setProfPhone`) Profil sahifasinikisi tomonidan **bosilardi** → Sozlamalarda ism/telefon o'zgartirilsa jimgina Profil sahifasining eski qiymatlari yozilardi (6-qoidaga zid). Sozlamalarniki **`settingsSaveProfile()`** ga ajratildi (+ bo'sh ism qorovuli); `saveProfile` — Profil sahifasiniki.
- ⚠️ **Namoz (`nmz*`) — BITTA modul bor, ikkinchisini YOZMA.** Asl modul (8–9 avgust, `e1e73f2..e1be870`): islomapi.uz → aladhan, sozlama **profilda** (`profiles.namoz_enabled/namoz_region/namoz_lat/lon`, `TASKFIX_NAMOZ.sql`), `NMZ_REGIONS` (12 viloyat + Qoraqalpog'iston + Toshkent sh.), `loadNamozSettings` (`bootstrap()`da, throw qilmaydi; ustun yo'q → `_nmzMissing`), toggle "☾ Muslim" (`#nmzToggle` → `nmzToggleClick`), `#nmzCtl`/`#nmzPanel`, `nmzDayBlocksHtml`/`nmzRenderPanel`/`nmzEnsureDays`, `nmzTickStart/Stop` (`goPage`da to'xtatiladi). **Tepadagi lentani yopish**: `#nmzPanel` ichidagi `×` → `nmzPanelSetHidden(true)`, holat `localStorage['nmz_panel_hidden']`; qaytarish — header'dagi `#nmzShowBtn` ("☾ Vaqtlar", faqat yopiq holatda ko'rinadi). ⚠️ Yopilganda **faqat lenta** yashirinadi — `nmzDayBlocksHtml` bloklari va modulning o'zi tegilmaydi. ⚠️ Yopiq bo'lsa `nmzTickStart` interval **boshlamaydi** va `nmzTickUpdate` to'xtaydi — aks holda `nmzCd` yo'qligi sababli har soniyada `nmzRenderPanel()` chaqirilardi. **Saboq (2026-08-11)**: eskirgan `main`dan boshlangan sessiya namoz kodini topmay ikkinchi (astronomik) modul yozdi → merge'dan keyin `const NMZ_TZ` ikki marta → **butun `<script>` parse bo'lmadi, ilova ochilmadi**; ustiga mening `.nmz-line`/`.nmz-chip` CSS'im keyinroq turgani uchun asl panelni bosardi. Takrori `a0fa44c`da o'chirildi. **Katta ish boshlashdan oldin `git fetch && git log origin/main` — yo'qdek ko'ringan feature aslida bor bo'lishi mumkin.**
- **Rejalashtirgich filtri (`plf*`)**: 5 chip — `none`/`created` (created_by) /`acceptor` (acceptor_id) /`assignee` (assigned_to + `_myTgBotIds`) /`done` (created_by + `status='completed'` + **sana oralig'i**, kun = `bajardi_at || completed_at`). ⚠️ **NISHONI: faqat chap "Bajarishim kerak" ro'yxati** (`plnSideTasks()` — ro'yxatning yagona manbasi). `applyTaskScope`ga **ULANMAYDI** — birinchi urinishda ulangan edi va chip bosilganda kanban ochilib ketardi (xato). Bar `#plnCalPane` ichida, faqat `schedule` tabida; sarlavha `#plnSideHead` filtr nomiga aylanadi. Chiplar **ICON_PATHS** ikonkalari bilan (`grid/send/badge/zap/checks`) — emoji yo'q. Sana oynasi = **ikki oylik kalendar** (`plfPopPaint`/`plfMonthHtml`/`plfCalPick`/`plfCalHover`): 1-bosish boshlanish, 2-bosish tugash (teskari bo'lsa almashadi), hover bilan oldindan ko'rsatish, 5 preset, Escape/tashqariga bosish (`_plfWatch`/`_plfKeyWatch`). ⚠️ `plfRender` oyna ochiq turganda barni **qayta qurmaydi** (`dataset.built`) — aks holda pop DOM'dan uchadi. ⚠️ `ICON_PATHS.chevron` **o'ngga** qaraydi → "oldingi" uchun `rotate(180deg)`. Holat `localStorage['plf_<ws>']`, `loadCurrentContext`da `plfLoad()`. CSS `.plf-*`.
- **Planner bloklari ustma-ust (`plnLanes`)**: kesishuvchi bloklar klasterga yig'iladi, har biriga lane beriladi, klasterdagi eng katta lane soni hammasiga qo'llanadi (Google Calendar uslubi) → `left:calc(lane*w% + 2px); width:calc(w% - 3px)`, `.pln-block.narrow` kichikroq shrift. Tegib turgan bloklar (`600 >= 600`) kesishgan hisoblanmaydi — ikkalasi to'liq kenglikda.
- **☾ Dam olish kuni ogohlantirishi (`dof*`)** *(2026-08-11, BRIEF_TASKFIX_LOYIHA.md 4-band)*: deadline **bajaruvchining** dam olish kuniga tushsa kehribar chip "☾ dam olish kuni". 🔴 Muddat **o'zgarmaydi**, hech narsa bloklanmaydi — faqat belgi. Modul global scope'da, `fmtDeadline` dan keyin.
  - **IKKI QATLAM** (A ustun, A bilmasa B): **A** = `employee_schedule_days` (haqiqat manbai, 41-migratsiya izohi) — faqat ko'rinayotgan `(uid, sana)` juftliklari; **B** = `employee_details` naqshi, **0 so'rov** (`loadHrData` keshlagan): `weekly`→`weekend_days`, `monthly`→`monthly_off_count/position` (43-migratsiya), `flexible`/naqshsiz → **`null` = noma'lum, ogohlantirish YO'Q**. `dofIsOff(uid,iso)` → `true|false|null`.
  - ⚠️ **`day_type` O'QILADI, `.eq('day_type','off')` bilan FILTRLANMAYDI**: filtrlansa "qator yo'q" holati *ish kuni* va *ma'lumot yo'q* ni farqlamaydi va B naqshi ish kunida **yolg'on chip** berardi. `start_time/end_time` umuman o'qilmaydi — 'off' kunda ham to'ldirilgan bo'lishi mumkin (manbada 4544 ta).
  - ⚠️ **RLS** `emp_sched_days_select` = `user_id = auth.uid() OR is_ws_manager(...)` → oddiy a'zo boshqa hodim uchun **bo'sh natija** oladi (xato emas) → jimgina B naqshiga tushadi. Menejer/owner aniq javob oladi.
  - ⚠️ **TOSHKENT kuni**: `dofDayIso` = `toLocaleDateString('en-CA',{timeZone:'Asia/Tashkent'})` (`nmz*` naqshi), brauzer mintaqasi EMAS — `employee_schedule_days.date` biznes kuni, deadline ham `+05:00` bilan yoziladi. `fmtDeadline` esa brauzer mintaqasida ko'rsatadi (chet elda kamdan-kam nomuvofiqlik bo'lishi mumkin — bilib qilingan). `toISOString().slice(0,10)` **ishlatilmaydi**.
  - **SO'ROV SHAKLI — byudjetli bloklar** (`dofBlocks`): har so'rovda `uid soni × sana soni <= DOF_BUDGET(800)`, `.in('user_id',…).in('date',…).limit(1000)`. ⚠️ `gte/lte` **oraliq ishlatilmaydi** — tarqoq sanalarda oradagi barcha kunlarni tortadi (o'lchov: 126 hodim × yil bo'ylab ≈ 46k qator, dekartdan ham battar). Byudjet ataylab PostgREST `db-max-rows` (odatda 1000) dan past.
  - 🔴 **Kesilish qorovuli**: `data.length >= DOF_LIMIT` → `_dofAsked` **belgilanmaydi** (qayta so'raladi), byudjet ikki barobar kichrayadi, bir marta `console.warn`. Jimgina kesilish A qatlamini yo'qotib B ga yolg'on chip berardi.
  - **Bloklamaydi**: render sinxron, `dofBadge(t)` noma'lum juftlikni navbatga qo'yadi (`dofWant`, hisoblagich `_dofQueueN` — `Object.keys` render ichida O(n) edi) → 80ms debounce → `dofFlush` → so'rov → **faqat nishon o'zgarsa** (`_dofDirty`) bir marta `dofRedraw()` (`refreshActiveView` + detal + create hint). Chegaralar: to'lqinda ≤400 juftlik / ≤6 so'rov, qolgani avtomat ≤5 turda. **Sikl yo'q**: so'ralgan juftlik (qator kelmasa ham) `_dofAsked` da qoladi.
  - Xato (jadval yo'q/RLS/tarmoq): bir marta `console.error`, `_dofMissing=true`, keyin jim — nishon ko'rsatilmaydi, ilova buzilmaydi (`loadEmailSettings` naqshi, throw yo'q). `dofClear()` — `loadCurrentContext` da.
  - Ko'rsatiladi: ro'yxat (`taskItemHtml`), vazifalar kanbani, bo'lim kanbani, loyiha bosqichi (`prjTaskCardHtml`), vazifa detali (nishon bo'lmasa qator **eski holida**), **yaratish oynasi** `#dofHint` (`dofCreateHint`: `cDeadlineDate` onchange, `setDeadlineRel`, `msUpdateSummary`, `plnOpenCreate`). `completed`/`cancelled` da chip yo'q. CSS `.dof-*`. **Yangi SQL kerak emas** — PK `(workspace_id,user_id,date)` so'rovni qoplaydi.
- **Login / auth sahifasi (`au*`)** *(2026-08-12)*: `#authShell` — chap brend paneli + o'ng karta, kartada **4 qadam** bir xil `.card` ichida `style.display` bilan almashadi: `#authStep` (kirish/ro'yxat), `#forgotStep`, `#resetStep`, `#inviteSetPwStep`.
  - 🔴 **Har qadam `<form onsubmit="event.preventDefault(); fn();">` ichida** — avval `<form>` umuman yo'q edi, ya'ni **Enter ishlamasdi**. Submit tugmalari (`#authBtn`, `#fBtn`, `#rBtn`, `#invBtn`) `type="submit"` va **`onclick` YO'Q** — ikkovi ham bo'lsa funksiya **ikki marta** chaqirilardi. Qolgan har tugmada (`#tabLogin/#tabSignup`, `.link-btn`, `.au-pweye`) `type="button"` **SHART** — form ichida sukut bo'yicha `submit`.
  - 🔴 **CSS `#authShell` bilan skoplangan**, eski 57–94-qatorlar (`.card`/`.field`/`.msg`/`.btn`/`.tabs-pill`) **o'chirilmagan** — ular butun ilovada (sozlamalar, modallar, `#loadingView`/`#onbBoardView`/`#bootErrView`) ishlatiladi. ID skopi (1,1,0) klassdan (0,1,0) kuchli → yangi blok stylesheet **boshida** tursa ham keyingi media query'lar uni bosa olmaydi; 1413-dagi eski 880px `.shell`/`.brand` qatorlari endi **o'lik** (ataylab qoldirilgan).
  - ⚠️ **`.msg` ga qo'shimcha klass BERILMAYDI** — `showMsg` `el.className = 'msg show ' + k` deb **qayta yozadi**, `hideMsg` esa `'msg'`. Shuning uchun ikonka `--au-ic` CSS o'zgaruvchisi + `mask` orqali `::before` da (`.err`/`.ok` o'zgaruvchini almashtiradi). `.msg.show` `display:grid` (16px ikonka + matn); yashirishni base `.msg{display:none}` bajaradi.
  - ⚠️ **Tugma spinneri `::after`** — `submitAuth` va boshqalar `btn.textContent = 'Tekshirilmoqda…'` yozadi, ya'ni **ichki element yashay olmaydi**. `.btn.block` `width:100%` bo'lgani uchun kenglik sakramaydi.
  - **A11y**: har `.msg` da `role="alert" aria-live="assertive"`; har inputda `label for=`; klaviatura fokusi `:focus-visible` bilan ko'rinadi — avval `.field input:focus{outline:none}` (0,2,1) global `input:focus-visible` (0,1,1) dan kuchli bo'lib **fokusni butunlay o'chirardi**.
  - **`auTogglePw(id, btn)`** — parolni ko'rsatish/yashirish, `aria-pressed` + `aria-label` almashadi, ikonka CSS bilan (`.au-ic-on/.au-ic-off`). 🔴 **top-level `function`** (inline `onclick` global scope'da baholanadi — 2026-07-28 saboqi).
  - 🔴 **`_authHideInvite()`** — 4 qadamdan faqat bittasi ko'rinsin. `showInviteSetPassword()` qolgan 3 tasini yashirardi, **teskarisi yo'q edi**: `showAuth`/`goForgot`/`showResetStep` `#inviteSetPwStep` ni yashirmay, ikki qadam birdan ko'rinishi mumkin edi (invite tokeni eskirib `SIGNED_OUT` kelganda).
  - Mobil: `≤880px` da brend paneli **ixcham lentaga** aylanadi (matn/feature yashirin), input `font-size:16px` — kichikroq bo'lsa iOS sahifani zoom qiladi. `≤480` da `.au-grid2` 1 ustun.
  - **JS mantiqi TEGILMAGAN**: `setMode`, `submitAuth`, `sendReset`, `saveNewPassword`, `saveInvitePassword`, `showMsg`/`hideMsg`, `switchToLoginKeepEmail` — bitta qator ham o'zgarmadi. `setMode` `#nameField`/`#forgotRow` ga `display:'block'` beradi; ular endi `.au-grid2` (grid) va `.au-lblrow` (flex) ichida — grid/flex bolasi uchun `block` to'g'ri ishlaydi. Dizayn spec: `LOGIN_SPEC.md` (scratchpad, repoda emas).
- **Landing (marketing) sahifasi — `landing.html`** *(2026-08-12)*: **ALOHIDA FAYL**, repo ildizida (~80 KB, o'zi-yetarli: CSS+JS ichida, tashqi ulanish faqat Google Fonts). Bo'limlar: header → hero + mahsulot mockup'i → ishonch lentasi → 3 zig-zag imkoniyat bloki → 8 modul kartasi → narxlar (Oylik/Yillik toggle) → FAQ (`<details>`) → to'q CTA → footer. CSS prefiksi **`.lp-*`**.
  - 🔴 **Nega alohida fayl**: `index.html` 1.2 MB — marketing mehmoni uni yuklamasligi kerak. Ilova **o'z joyida** (`index.html`) qoldi, ya'ni Supabase Site URL / parol tiklash / taklif havolalari **hech qanday o'zgarishsiz** ishlaydi. Landing'ni ildizga ko'chirish (`index.html` = landing, ilova = `app.html`) **ataylab qilinmadi** — auth redirect'lari taskfix.org ildiziga keladi va ularni sindirish xavfi bor edi.
  - 🔴 **Darvoza (`index.html` `<head>`, 16–39-qatorlar)** — sinxron IIFE, fonts/`<link>` lardan **oldin**. Mehmonni `location.replace('landing.html')` qiladi. Tamoyil: **shubha bo'lsa ILOVADA QOLAMIZ** (butun tanasi `try/catch`, `localStorage` throw qilsa yo'naltirmaydi). 5 ta o'tkazish sharti: `?app=1` · hash'da `access_token|refresh_token|type=|error_description|error=` · query'da `invite|code|token_hash|type|error|error_description` · `#/`-prefiksli chuqur havola yoki `#login`/`#signup` · localStorage'da `sb-*-auth-token`.
    - ⚠️ **`error` query'da HAM tekshiriladi**: PKCE/`/verify` oqimida eskirgan reset/invite havolasi xatoni hash'da emas **query'da** qaytaradi (`?error=access_denied&...`) — busiz odam kirish ekrani o'rniga landingga tushardi (tester 2026-08-12 da tutdi).
    - ⚠️ `location.replace` query'ni **tashlaydi** (UTM yo'qoladi) — reklama kampaniyasi to'g'ridan `landing.html` ga link qo'ysin.
    - ⚠️ Sessiya kaliti umumiy `sb-*-auth-token` naqshi bilan qidiriladi (loyiha ref'iga bog'lanmagan) — xavfsiz tomonga og'adi (ilovada qolish).
  - **Havolalar**: landing CTA'lari **faqat** `index.html?app=1#login` / `index.html?app=1#signup`. `?app=1` darvoza uchun, `#signup` esa `bootstrap` oxirida `setMode(_rawHash === '#signup' ? 'signup' : 'login')` bilan darrov "Ro'yxatdan o'tish" tabini ochadi (`_rawHash` — `createClient` DAN OLDIN o'qilgan nusxa, `location.hash` emas). Teskari yo'l: `#authShell` dagi `.wordmark` ichi `<a href="landing.html" class="au-wm-link">` (o'ram `.wordmark` divning **ichida** — tashqarisida bo'lsa flex-item almashib layout siljirdi).
  - ⚠️ **`.lp-fl li` grid (`22px 1fr`)** — matn ichida `<b>`/`<em>` bo'lsa u **alohida grid katagiga** tushib qatorni buzadi (anonim grid item). Shuning uchun bunday qatorda butun matn **bitta `<span>`** ichida bo'lishi shart.
  - Mahsulot "skrinshot"lari — **CSS+SVG mockup**, rasm fayli YO'Q (GitHub Pages'da hech qachon buzilmaydi). Keng mockup `.lp-scroll{overflow-x:auto}` ichida; `html,body{overflow-x:clip}` (`hidden` EMAS — sticky header buzilmasin).
  - Ikonkalar — `index.html` dagi `ICON_PATHS` bilan bir xil Lucide geometriyasi, inline SVG. Emoji yo'q.
  - 🔴 **NARX KO'RSATILMAYDI** *(2026-08-12, Asilbek qarori)*: bo'lim "Narxlar" emas **"Rejalar"** (`#pricing` anchor'i O'ZGARMADI — havolalar buzilmasin). Uchala rejada (Boshlang'ich / Biznes / Korxona) faqat **"Bog'lanish"** tugmasi → `https://t.me/data_aros`. Oylik/Yillik toggle, `.lp-amount`/`.lp-cur`/`.lp-per`/`.lp-toggle`/`.lp-save` CSS'i va `setCycle` JS'i **butunlay olib tashlandi**. `.lp-price .lp-for { min-height: 40px }` — tugmalar bir chiziqda tursin.
    - **Butun sahifada aloqa nuqtasi BITTA**: `t.me/data_aros` (12 havola — hero, 3 reja, FAQ lentasi, yakuniy CTA, footer "Kompaniya" 4 ta + "Aloqa"). `mailto:` YO'Q, boshqa Telegram username YO'Q. Har `t.me` havolasida `target="_blank" rel="noopener noreferrer"`.
    - Bepullik da'volari ham olib tashlandi ("Bepul boshlash" → **"Ro'yxatdan o'tish"**, "5 kishigacha bepul" → "Jamoangizga moslanadi") — narx yo'q sahifada bepullik va'da qilib bo'lmaydi. Ro'yxatdan o'tish/kirish havolalari O'ZGARMADI.
  - **Ochiq (Asilbek hal qilsin)**: footer'da "Maxfiylik siyosati"/"Foydalanish shartlari" **umuman yo'q** (Kompaniya ustuni Telegram havolalariga almashtirildi); `og:image` yo'q; login sahifasidagi "14 kun bepul" landing bilan **zid** (landing endi bepullik va'da qilmaydi).
- **📱 MOBIL + PWA (`mob*` / `pwa*`)** *(2026-08-15, spec: `MOBILE_SPEC.md` — scratchpad, repoda emas)*: butun ilova telefonga moslashtirildi va PWA qilindi. Yangi fayllar: **`manifest.json`**, **`sw.js`**, **`icons/`** (5 PNG).
  - 🔴 **Nima buzuq edi**: eski `@media(max-width:880px)` bloki `.side-section, .side-spacer, .side-bottom { display:none }` yozardi — mobilda **bo'limlar, Loyihalar, Boshqaruv, Telegram/HR guruhlari, Sozlamalar, profil, 🔔, Chiqish umuman ochilmasdi**. O'chirildi.
  - **Navigatsiya = off-canvas drawer** (pastki tabbar EMAS): menyu dinamik (`renderSidebar` da 8 shartli shox), shuning uchun faqat drawer har punkt joyida qolishini kafolatlaydi. Mavjud `<aside class="sidebar">` HTML'i **1 belgi ham o'zgarmadi** — u drawer bo'lib ochiladi (`#appSidebar`, `#mobScrim`). `MOB_W=880` = `SB_MOBILE_W` (bitta chegara, `sbApply` bilan to'qnashmaydi).
  - 🔴 **`mobNavOnClick` CAPTURE fazasida bog'lanadi** (`side.addEventListener('click', fn, true)`), bubble EMAS: `#tgConnBtn`, `.profile-menu-item`, 🔔 da inline `onclick="event.stopPropagation(); …"` bor va bubble'ni to'xtatadi → delegatsiya **hech qachon ishlamasdi**. Simptom: drawer'dan "Bo'limlar +" yoki ✈ bosilganda modal (`z-index:100`) drawer (8600) ortida ochilib **ko'rinmasdi**. Yopadi: `.side-link`, `.profile-menu-item`, `.ws-item`, `.add-mini`, `#tgConnBtn`, `#pwaDrawerBtn`. Yopmaydi: `.side-link.side-group` (Telegram/HR), 🔔 (paneli 8710 = drawer ustida), `.ws-switch`/`.side-user` o'zi.
  - 🔴 **`html.mob-lock` (scroll qulfi) — yagona egasi `mobLockSync()`**. Uch bayroqdan qayta hisoblaydi: `_mobNavOpen`, `body.mob-search-open`, `body.mob-ss-open`. Har biri o'zi qo'ysa/olsa, ikki qoplama birga ochilib bittasi yopilganda qulf erta yechilar yoki abadiy qotib qolardi.
  - 🔴 **`transform` `body`/`#appView` ga HECH QACHON berilmaydi** — u containing block yaratadi va `position:fixed` bo'lgan 8 panel (`.ss-panel` 9100, `.ui-modal-bd` 9000, `.gs-drop`, `.toast`, `.tsk-chip`, `.modal-backdrop`, `.notif-panel`, `.tbl-filter-pop`) noto'g'ri joyga chiqadi. Faqat `.sidebar` ning o'zi siljiydi.
  - 🔴 **`#topSearchWrap` `display:none` QILINMAYDI** — `gsPosition()` uning `getBoundingClientRect()` ini o'qiydi, 0 bo'lsa qidiruv jimgina o'ladi. Yopiq holatda `position:absolute + visibility:hidden`.
  - 🔴 **Klaviatura (`visualViewport`)**: `mvvApply()` → `--mob-vh` / `--mob-vvtop` / `--mob-kb`. Modal backdrop'lari `top/height` ni shundan oladi → varaq qisqargan maydon tagiga yopishadi va **"Saqlash" doim ko'rinadi**. `.ss-panel.sheet`, `.gs-drop`, `.emp-filter-panel` `bottom: var(--mob-kb,0px)` ishlatadi (iOS layout oynasini qisqartirmaydi → `bottom:0` klaviatura ostida qolardi). `dvh` — zaxira.
  - **`ssPosition()` mobil early-return** (≤`SS_SHEET_W=640`): inline `left/top/bottom/width/maxHeight` **tozalanadi** va `.sheet` klassi qo'yiladi — aks holda oldingi ochilishdan qolgan koordinata varaqni buzardi. Scrim = `body.mob-ss-open::before` (z **9090**, ui-modal 9000 va panel 9100 orasida), alohida DOM element yo'q. Qidiruv/klaviatura mantig'iga tegilmagan.
  - **Z-QATLAM (yangilangan)**: `20 topbar · 90 gs-drop va FAB · 100 modal-backdrop · 400 qidiruv modal · 8000 tsk-chip · 8500 mob-scrim · 8600 drawer · 8700/8710 notif scrim/sheet · 8800/8810 pwa-ios · 9000 ui-modal-bd · 9090 ss-scrim · 9100 ss-panel · 9400 perf · 9500 toast`. ⚠️ FAB ataylab **90**, spec'dagi 8300 EMAS — 8300 bo'lsa ochiq modal (100) ustida qalqib turardi; `.topbar` `sticky+z:20` o'z stacking kontekstini yaratgani uchun FAB ildizda baribir 20-daraja.
  - **Komponentlar**: kanban → gorizontal snap-scroll (faqat ≤880); jadval → gorizontal scroll + yopishqoq 1-ustun (`.col-freeze`); modallar → bottom sheet; chip qatorlari → `overflow-x` scroller ⚠️ **`.plf-bar`/`.emp-filter-bar` dan TASHQARI** (ichlarida absolute popover bor, `overflow` ularni kesardi — o'rniga popoverning o'zi ≤640 da `position:fixed` varaq bo'ldi); planner setkasi saqlandi (`.pln-cal` ichida scroll, `.pln-timecol` sticky); org chart — `touch-action: pan-x pan-y` + **qo'lda pinch-zoom** (`oscBindPan` da, `oscZoomSet` bilan bir xil langar — `touch-action` brauzer pinch'ini o'chirgani uchun CSS yetmaydi).
  - **Teginish**: ≤880 da barcha nishonlar ≥44px; `input/textarea/select { font-size:16px !important }` (iOS fokusda zoom qilmasin) — `!important` SHART, 272 inline `font-size` ning bir qismi JS shablonlarida generatsiya qilinadi; hech bir maydonda inline ≥16px yo'q, ya'ni hech narsa bosilmaydi. `@media (hover:hover)` / `(hover:none)` — sensorli ekranda amallar ko'rinadi.
  - **Jamoa jadvali**: `tm-c-num`/`tm-c-av` ≤880 da yashiriladi va "To'liq ism" `col-freeze` bo'ladi — muzlatish 3-ustunga berilgani uchun oldingi ikkitasi yashirilishi **shart** (`left:0` aks holda ular ustiga chiqardi). ⚠️ `renderHodimStats` jadvalida muzlatish YO'Q (ochiq ish).
  - **PWA**: `manifest.json` (`.webmanifest` EMAS — GitHub Pages MIME kafolati), `start_url: "index.html?app=1"` 🔴 **`?app=1` MAJBURIY** — busiz landing darvozasi (16–39-qator) o'rnatilgan ilovani `landing.html` ga yuborardi; `id: "/"` (CNAME `taskfix.org` ildizda); `theme_color`/`background_color` = `#FAF7F0` (topbar foni bilan bir xil, "suzuvchi qora lenta" bo'lmasin).
    - **Manifest IKKALA sahifaga ulanadi** — `beforeinstallprompt` faqat manifest shu sahifada ulangan bo'lsa keladi; `start_url` baribir ilovani ochadi.
    - `sw.js`: HTML **network-first** (1.2 MB ilova eski qotib qolmasin) + `NAV_TIMEOUT=4500ms` da keshdan + fonda yangilash + sahifaga `{type:'tf-updated'}` → toast (sessiyada bir marta). 🔴 **Supabase HECH QACHON keshlanmaydi** — faqat shu origin + `ALLOW_HOSTS` (jsdelivr/cdnjs/Google Fonts); POST/PATCH va `Range` tegilmaydi. 🔴 **`index.html` precache QILINMAYDI** (SW landing'dan ham ro'yxatga olinadi → har marketing mehmoni 2.5 MB yuklab olardi). 🔴 `e.waitUntil(net)` birinchi `await` DAN OLDIN sinxron — busiz sekin 3G'da fon yangilanishi tugamay SW to'xtab, kesh **hech qachon yangilanmasdi**. Deploy'da `CACHE_VER` oshiriladi.
    - O'rnatish UI'si 3 joyda: landing pastki lenta `.pwa-bar` · ilovada drawer punkti `#pwaDrawerBtn` + topbar ostidagi `.pwa-inapp` banneri (faqat 2-ochilishdan, `pwa_seen>=2`) · iOS uchun 3 qadamli `.pwa-ios` varag'i (`beforeinstallprompt` yo'q; `CriOS|FxiOS|EdgiOS|OPiOS` istisno). `localStorage`: `pwa_dismiss_v1` (14 kun), `pwa_installed_v1` — **ikkala sahifada bir xil**; `pwa_seen` faqat ilovada. Standalone'da **hech qanday o'rnatish UI'si ko'rinmaydi** (CSS + JS ikki qatlam).
    - 🔴 **`pwa_installed_v1` HAKAM EMAS — `beforeinstallprompt` hakam** *(2026-08-15, Asilbek topgan bug)*: brauzer bu hodisani FAQAT ilova o'rnatilmagan va o'rnatilishi mumkin bo'lganda otadi. Shuning uchun (a) `pwaCanInstall()` da `_pwaPrompt` bor bo'lsa bayroqqa **umuman qaralmaydi**, (b) hodisa kelganda eskirgan bayroq **tozalanadi** (`pwaLsRemove`/`lsRemove`). Busiz: odam ilovani o'rnatib, keyin telefonidan **o'chirsa**, bayroq localStorage'da qolib "Ilovani o'rnatish" tugmasi **abadiy yo'qolardi** — foydalanuvchi Chrome menyusidan qidirishga majbur bo'lardi (hamma ham bilmaydi). Bayroq endi faqat **iOS** uchun ma'noli (u yerda `beforeinstallprompt` ham, `appinstalled` ham yo'q → aniqlashning boshqa yo'li yo'q).
    - `--mob-topbar-h` (56px; standalone'da `+env(safe-area-inset-top)`) — `.osc-chart-bar` `top` i shundan oziqlanadi.
  - 🔴 **Asilbek qarori (2026-08-15) — DESKTOP TEGILMAYDI**: kanban gorizontal ko'rinishi va landscape bloklari `980px` emas **`880px`** bilan chegaralangan (881–980 = noutbukning toraytirilgan oynasi, telefon emas — u yerda eski vertikal ko'rinish qoladi); `#pwaDrawerBtn` `pwaSyncUi` da `innerWidth <= MOB_W` sharti bilan (desktop chap menyusida yangi punkt paydo bo'lmasin). Ikkalasi ham dizayn spec'i ruxsat bergan, lekin so'ralgani mobil edi.
- **Routing**: `goPage`, `rtSetHash`/`rtRouteFromHash` (hash-routing, popstate).
- **Chap menu yig'ish (`sb*`)**: `sbInit()` (init'da), `sbToggle()` (brand-top dagi `#sbToggleBtn` + **Ctrl/Cmd+B**), `sbApply()`, `sbTitles()`. Holat `localStorage['sb_collapsed']='1'|'0'`; qiymat **YO'Q = foydalanuvchi tanlamagan** → ekran kengligiga qarab avtomatik (`SB_AUTO_W=1280`), bir marta bosgach tanlovi ustun (resize buzmaydi). `SB_MOBILE_W=880` dan tor ekranda qo'llanmaydi (sidebar u yerda gorizontal panel) — JS ham, media query'da zaxira override ham bor. CSS `.sb-collapsed` `#appView` da. ⚠️ Menyu matni alohida span'da EMAS (matn tuguni) → `.side-link{font-size:0}` + `.ic` da qaytariladi; matn hover'da `title` orqali (`sbTitles`, faqat matn tugunlarini yig'adi). `renderSidebar()` oxirida `sbTitles()` chaqiriladi.
- **Qidiruvli tanlash (`ss*`)** *(2026-08-06)*: butun ilova uchun **yagona** tanlash komponenti — `uiForm` maydon turi `search` (bitta) yoki `msearch` (ko'p). API: `ssFieldHtml(id,{items,value,multi,placeholder,empty})` → tugma HTML; `ssValue(id)` → single `'x'` / multi `['x','y']`; `ssForget(ids)` modal yopilganda tozalaydi. `items:[{value,label,sub,uid,search}]` — `uid` berilsa avatar, `search` qo'shimcha matn (telefon/email). Eski `options:[{value,label}]` ham qabul qilinadi.
  - **Qidiruv**: label + sub + search bo'yicha; 3+ raqam kiritilsa **telefon** bo'yicha ham (ikki tomon raqamlargacha normallashtiriladi — probel/plyus/tire ahamiyatsiz).
  - **Xodim tanlash**: `ssMemberItems(list)` — avatar + lavozim (`memberPos`) + telefon/email qidiruvi, alifbo tartibida, o'zi "Men" deb ko'rsatiladi. Hamma joyda shu ishlatiladi (bir xil ko'rinish/xatti-harakat).
  - ⚠️ Panel `<body>` ning bevosita farzandi + `position:fixed` — `gs*` dagi bilan bir xil sabab. Bir vaqtda **bitta** panel (`#ssPanel` qayta ishlatiladi, DOM ko'paymaydi); ochilganda `body` oxiriga ko'chiriladi.
  - 🔴 **Z-INDEX QATLAM XARITASI** (2026-08-06 da bug tuzatildi — panel `200` da edi va modal ortida qolib ko'rinmasdi): `200` eski qatlamlar · `400` qidiruv modali · `8000` `.tsk-chip` · **`9000` `.ui-modal-bd`** (uiForm/uiConfirm, `backdrop-filter` bilan) · **`9100` `.ss-panel`** · `9400` perf paneli · **`9500` `.toast`**. Yangi qoplama qo'shsangiz shu xaritaga qarab tanlang. ⚠️ `body` da `transform`/`filter` YO'Q — shuning uchun `position:fixed` to'g'ri ishlaydi; kelajakda qo'shilsa panel buziladi.
  - **Joylashuv** (`ssPosition`, `SS_MAX_H/GAP/EDGE/ROW_H`): pastda joy yetmasa **tepaga** ochiladi; ikkala tomon ham tor bo'lsa kattarog'i tanlanadi va panel o'sha joyga **sig'diriladi** (ichida scroll); chap/o'ng chekkadan chiqmaydi. Balandlik **ochilganda bir marta** variantlar soniga qarab hisoblanadi — qidiruv paytida qayta hisoblansa panel "sakrardi". Scroll (`capture:true`) va resize'da qayta joylashadi.
  - Klaviatura: ↑/↓ + Enter + Esc. Enter `stopPropagation` qiladi — `uiForm`ning "Enter → submit" i ishlab ketmasin. Tashqariga bosish listener'i **bir marta** bog'lanadi (`_ssBound`).
  - CSS `.ss-*`.
- **HR Service (`hrsv*`)** *(2026-08-06)*: "Qo'shimcha service" sahifasidagi ikkinchi karta (Telegram yonida). Holat `public.hr_settings` da — **`integrations` jadvaliga QO'SHILMADI** (u yerdagi `tool` CHECK'ini o'zgartirish mavjud Telegram/n8n integratsiyasiga xavf tug'dirardi; HR tashqi xizmat emas, ichki modul). `loadHrSvcSettings()` — `loadCurrentContext` 1-to'lqinida, **hech qachon throw qilmaydi**. Jadval yo'q → `_hrsvMissing=true` va **HR O'CHIQ** deb hisoblanadi (email sozlamasidan farqli: yangi modul jimgina yoqilib ketmasin, aks holda ishlamaydigan menyu chiqardi). `hrsvSetEnabled` upsert + `.select()`. Karta 4 holat ko'rsatadi: Sozlanmagan (SQL yo'q) / O'qilmadi / Yoqilgan / Yoqilmagan. Yoqilsa chap menyuda **HR** guruhi (Telegram naqshi: `side-group`+`side-sub`, `toggleHrSidebar`, localStorage `sb_hr_v1`, default YOPIQ) → "Org Schema".
- **HR muharriri (`hrEd*` / `hrIsEditor`)** *(2026-08-13)*: owner/admin bo'lmagan xodim **Org Schema'ni tahrirlay olsin**, lekin qolgan hamma joyda oddiy a'zo bo'lib qolsin. `TASKFIX_HR_EDITORS.sql` (**RUN kutilmoqda** — busiz hech narsa o'zgarmaydi, ilova hozirgidek ishlaydi).
  - 🔴 **Ruxsat ODAMGA emas, LAVOZIMGA bog'lanadi**: `positions.hr_editor boolean NOT NULL DEFAULT false`. Owner "HR" lavozimini bir marta belgilaydi → o'sha lavozimdagi HAR KIM tahrirlaydi; xodim almashsa ruxsat o'zi ko'chadi. **Lavozim NOMI bo'yicha moslashtirish ATAYLAB ishlatilmadi** ("HR" / "HR menejer" / "Kadrlar bo'limi" — mo'rt).
  - **Nega o'zini o'zi HR qila olmaydi**: `positions_write` = `is_ws_manager` va `emp_details_write` = `is_ws_manager` (xodim **o'z** `employee_details` qatorini ham tahrirlay olmaydi, `39_employee_details.sql:158`). Ya'ni bayroqni ham, `position_id` ni ham faqat owner/admin qo'yadi. UI'da ham `hrEdRowHtml`/`hrEdOpen`/`hrEdSave` uchtasi `isOwnerLike()` bilan yopiq.
  - 🔴 **`hr_is_editor(p_ws, p_user)` — SECURITY DEFINER, tanasida IKKI MAJBURIY qorovul**: (1) `p_user = (SELECT auth.uid())` — funksiya PostgREST `/rpc/hr_is_editor` orqali ham chaqiriladi (`can_see_project` naqshi); (2) **`is_ws_member(p_ws, p_user)`** — busiz **jamoadan chiqarilgan xodim yozish huquqini SAQLAB QOLARDI: `rmxDoRemove` uni faqat `workspace_members` dan chiqaradi, `employee_details` qatori bazada QOLADI** (CASCADE yo'q, mijoz ham o'chirmaydi) → lavozimi hamon `hr_editor` bo'lib, u to'g'ridan-to'g'ri API orqali `org_*` ga yoza/**o'chira** olardi (SELECT to'silgani uchun **ko'rmasdan** buzardi). Mavjud policy'lar `is_ws_manager()` orqali bu invariantni saqlardi — yangi policy uni buzmasligi shart. SQL'ning 4.b2 tekshiruvi funksiya **kodida** (izohlar `regexp_replace` bilan tozalab) ikkala qorovul borligini talab qiladi, yo'q bo'lsa `RAISE EXCEPTION`.
  - **Policy'lar ADDITIVE**: 6 ta yangi `AS PERMISSIVE` (`org_folders_insert_hr/_update_hr/_delete_hr`, `org_people_*`). Eski 6 tasiga **tegilmaydi** (nomlar boshqa → `TASKFIX_HR.sql` qayta run qilinsa ham buzilmaydi). SELECT policy qo'shilmagan — ko'rish avvaldan ochiq.
  - **Mijozda yagona choke-point**: `oscCanEdit() { return isOwnerLike() || hrIsEditor(); }` — `osc*` da 20 ta chaqiruv shundan o'tadi. `hrIsEditor()` 0 qo'shimcha so'rov qiladi (`empDetails[me.id].position_id` → `positionsCache` → `!!p.hr_editor`); kesh yo'q/`hr_editor` undefined → **`false`** (xavfsiz sukut). ⚠️ `hrIsEditor()` **boshqa hech qayerda ishlatilmaydi** — HR muharriri loyiha/sozlama/xodim boshqaruviga tegmaydi.
  - ⚠️ **`positions` so'rovi `select('*')`** (`loadHrData`): `hr_editor` ni nomma-nom so'rasak SQL run qilinmagan bazada **butun so'rov 42703 bilan yiqilib lavozimlar ILOVANING HAMMA joyidan yo'qolardi**. `*` bilan ustun yo'q bo'lsa kalit shunchaki kelmaydi.
  - ⚠️ **`loadHrData` FONDA ishlaydi** → Org Schema ochilganda ruxsat hali noma'lum bo'lishi mumkin. Shuning uchun `.then` da (muvaffaqiyat **va** xato shoxlarida) `currentPage==='orgschema' && !_oscLoading` bo'lsa qayta chiziladi. Yuklash yiqilsa (`_hrError`) `oscRenderActions` sababni + **«Qayta urinish»** (`oscRetryHr`) ko'rsatadi — busiz HR muharriri "tugmalarim yo'q" deb sababsiz qolardi.
  - ⚠️ `hrEdSave` ikkita UPDATE qiladi (**tranzaksiya yo'q**) — biri o'tib ikkinchisi yiqilsa kesh DB'dan farq qilib qolardi, shuning uchun `catch` da `loadHrData(true)` bilan serverdan qayta o'qiladi. Har UPDATE `.select('id')` + qator sonini tekshiradi (6-qoida).
  - **Kelajakdagi HR subsection'lari uchun**: `hr_is_editor()` — yagona nuqta. Yangi jadvalga bitta PERMISSIVE policy (`… OR hr_is_editor(workspace_id, (SELECT auth.uid()))`) va mijozda `hrIsEditor()` yetadi. `hrsvSetEnabled` (HR Service'ni yoqish/o'chirish) **owner-only qoladi**.
- **Org Schema (`osc*`)** *(2026-08-06)*: `page-orgschema`, ikki ko'rinish bitta manbadan (`org_folders` + `org_people`, TASKFIX_HR.sql).
  - **Papka daraxti**: papkalarni foydalanuvchi **noldan o'zi yaratadi**. ⚠️ *2026-08-06 qarori*: TaskFix'dagi mavjud `departments` Org Schema'ga **AVTOMAT TORTILMAYDI** — `org_sync_departments()` RPC ham, `org_dept_rename_sync` trigger'i ham olib tashlandi (SQL'da `DROP IF EXISTS` bilan tozalanadi, tekshiruv bloki ular YO'Qligini tasdiqlaydi). Natijada TASKFIX_HR.sql `departments` jadvaliga **umuman tegmaydi** (FK ham qolmadi). Har qanday papkani o'chirish mumkin. `org_folders.kind`/`department_id` **ustunlari olib tashlandi** (SQL avval avtomat yaratilgan `kind='department'` papkalarni DELETE qiladi, keyin ustunlarni DROP qiladi — tekshiruv bloki ular yo'qligini tasdiqlaydi).
  - **🔴 BITTA BIRLASHGAN DARAXT (`oscTree()`)**: tugun papka ham, hodim ham bo'ladi va **har qanday tugun pastga davom etadi**. Ota polimorf, lekin **haqiqiy FK bilan** (ustun juftligi, `parent_kind text` EMAS — u FK yozishga imkon bermasdi va o'chgan tugun "osilib" qolardi):
    - `org_folders`: `parent_id` (papka ostida) **yoki** `parent_person_id` (hodim ostida)
    - `org_people`: `folder_id` (papkada) **yoki** `parent_person_id` (hodim ostida)
    - `CHECK` ikkovi bir vaqtda to'lmasin; `ON DELETE SET NULL` (CASCADE emas) — rahbar olib tashlansa uning ostidagi shox **o'chib ketmaydi**, ildizga ko'chadi.
    Tugun kaliti `oscKey(type,id)` → `'f:<id>'` / `'p:<id>'` (ikki jadval id'lari to'qnashmasin). Yordamchi: `oscSubtreeKeys` (avlod, sikl himoyali), `oscNodeOptions` (ota tanlash ro'yxati), `oscParentPatch`/`oscPersonParentPatch`.
  - **🔴 BO'YSUNISH — daraxtdan hisoblanadi, saqlanmaydi** (`manager_id` YO'Q, qo'lda tanlash yo'q): otasi **hodim** bo'lsa → o'sha hodim; otasi **papka** bo'lsa → o'sha papkadagilar **tengdosh** (o'zi ham shu yerda!), shuning uchun papka **o'tkazib yuboriladi** va yuqoridagi eng yaqin hodim(lar) rahbar bo'ladi; bo'sh papkalar ham o'tkaziladi; boshqa ildiz bilan bog'lanish yo'q. `oscDerivedManagers(person, tree)` → `oscMgrBadge`. ⚠️ "O'z papkasini o'tkazib yuborish" qadami MUHIM — busiz hodim o'zini va tengdoshlarini rahbar deb ko'rsatardi (2026-08-06 da aynan shu xato test bilan tutildi).
  - **Sikl**: daraxt ikki jadval bo'ylab yoyilgani uchun sikl **ARALASH** bo'lishi mumkin (papka→hodim→papka). Server tomonda yagona `org_check_ancestor(kind,id,pkind,pid,ws)` funksiyasi ikkala trigger'dan (`org_folders_guard_trg`, `org_people_guard_trg`) chaqiriladi. SQL tekshiruv bloki sof va aralash siklni **tirik** sinaydi.
  - **Tugun qatori** (`oscNodeRowHtml`, papka ham hodim ham): butun qator bosiladi → **faqat shu tugun** ochiladi/yopiladi (accordion EMAS). Chevron — bezak; amal tugmalari `event.stopPropagation()`. Klaviatura: `role="button"`+`tabindex`+`oscRowKey`. Har tugunda "+ hodim" va "+ papka".
  - **🔴 VIZUAL BIRLASHTIRISH (org chart)**: papkada **atigi bitta** hodim bo'lsa, papka va hodim ikki daraja emas, **bitta kartochka** bo'lib chiziladi ("CEO · Feruzbek"), bolalari = papkaning qolgan bolalari + o'sha hodimning bolalari. 2+ hodim bo'lsa eskicha (papka tuguni + ichida hodimlar). ⚠️ Bu **faqat chizish** — DB ham, `oscTree()` ham o'zgarmaydi; tahrir tugmalari ikkovini alohida boshqaradi (`type:'merged'`, CSS `.osc-card.merged`).
  - **Org chart** (`oscChartRoots`/`oscLayout`/`oscElbow`): `oscTree()` dan o'qiydi. Avto algoritm: barglar qat'iy ustun egallaydi, ota bolalari ustida markazlashadi → ustma-ust tushish mumkin emas.
  - **🔴 Chart: zoom / drag / qo'lda joylashuv** *(2026-08-08, BRIEF_TASKFIX_ORGCHART.md)*:
    - **Zoom faqat chartga**: `oscWheel` — `oscViewport` da `wheel` (`passive:false`) + `preventDefault`. Busiz trackpad pinch brauzerga `ctrl+wheel` bo'lib borib **butun sahifani** zoom qilardi (aynan shu bug edi). Kursor ostida langar (`oscZoomSet(z,ax,ay,instant)`), `deltaMode` normallashtiriladi, chegara `OSC_ZMIN/MAX = 0.3…2.5`. ⚠️ Sahna `#oscCanvas` ichida: `transform` layout o'lchamini o'zgartirmagani uchun canvas `natural × zoom` qilib beriladi — busiz 100% dan katta masshtabda o'ng/past chekkaga scroll qilib bo'lmasdi.
    - **Drag**: karta ustidan `mousedown` → pan emas, surish (faqat `oscCanEdit()`, `.osc-cact` ustidan emas). Ostidagilar birga siljiydi; chiziqlar `oscEdgesPath` + rAF bilan real vaqtda qayta chiziladi; `delta / oscZoom`. 3px dan kam siljish = klik; ko'proq bo'lsa `oscCardOpen` 300ms ichidagi klikni yutadi (`_oscDragEndAt` — bayroq emas, VAQT: surish oxirida chart qaytadan chiziladi).
    - **Qo'lda joylashuv saqlanadi** — ⚠️ **2026-08-06 dagi "`node_x/node_y` SAQLANMAYDI" qarori foydalanuvchi talabi bilan BEKOR QILINDI**. `TASKFIX_ORGCHART.sql` (**RUN kutilmoqda**): `org_people` VA `org_folders` ga `node_x/node_y double precision` + `CHECK ((node_x IS NULL) = (node_y IS NULL))`. NULL = avto. Birlashgan kartochka joyi **papka** qatorida. `oscLayout` ikki qatlamli: avto (`ax/ay`) → ustidan qo'lda (`oscManualPos`), **avlod ota siljigan miqdorga siljiydi** lekin avto bo'lib qoladi; surilgan tugunning avto o'rni ataylab bo'sh qoladi (sxema "sakramasin"). ⚠️ `node_x/node_y` **ma'no tashimaydi** — bo'ysunish avvalgidek faqat `parent_*`/`folder_id` dan.
    - Saqlash `oscSaveNodePos` (`.select()` + `{error}`, xatoda karta **eski joyiga qaytariladi**). Ustun yo'q bo'lsa (`42703`/`PGRST204` → `_oscPosMissing`) — bir marta toast, surish sessiya ichida ishlayveradi. Qaytarish: kartada ↺ (`oscResetNode`, faqat `n.manual` da) va panelda "Avto joylashuv" (`oscResetLayout`, tasdiq bilan, mahalliy kesh **muvaffaqiyatdan keyin** tozalanadi).
    - Zoom/pan: `oscZoomFit/Set/By`, `oscBindPan` — window listener'lari **bir marta** (`_oscPanBound`), viewport listener'lari (wheel/mousedown) har chizishda yangi elementga.
  - Yozuv amallari faqat `isOwnerLike()`; **ko'rish har a'zoga ochiq** (RLS ham shunday) — shu sababli `orgschema` `managerOnlyPages` da EMAS, o'rniga `goPage` da `hrsvEnabled()` tekshiruvi.
  - Sikl himoyasi ikki qatlamda: server trigger `org_folders_guard_trg` (yagona haqiqiy himoya) + mijozda `seen` to'plami (buzilgan ma'lumot sahifani yiqitmasin). ⚠️ `org_people_guard` **olib tashlandi** — bo'ysunish saqlanmagach kerak emas.
  - CSS `.osc-*` (810–894-qatorlar, moslashtirish blokidan OLDIN — qoida saqlangan).
- **⚠️ Eski "Tashkilot tuzilmasi" (`oc*` / `org_containers`)** — *2026-08-06 dan vaqtincha YASHIRILGAN*, o'chirilmagan. Jamoa sahifasidagi **butun tab qatori** (`view-tabs`: "👥 Hodimlar" + "🏢 Tashkilot") HTML izohiga olindi — Tashkilot HR'ga ko'chgach bitta tab qolib ma'nosiz bo'lgandi; `teamPaneHodimlar` endi to'g'ridan-to'g'ri ko'rinadi. `gsPushPages` dagi "Tashkilot tuzilmasi" yozuvi ham izohda. `teamSetTab()` saqlanmoqda (`goPage('team')` uni 'hodimlar' bilan chaqiradi). Kod ham, DB ma'lumoti ham joyida; qaytarish uchun izohni ochish yetarli.
- **Ikonkalar (`ic*`)**: `ICON_PATHS` (23 ta, Lucide geometriyasi, 24×24) — 2026-08-06 da qo'shildi: `hr` (contact-round), `network`, `folderOpen`, `trash`, `pencil`, `userPlus`. + `icon(name,size)` + `sideIcon(name)`. **Inline SVG, tashqi CDN YO'Q** (build yo'q + CDN beqaror). Noma'lum nom → `console.warn` + bo'sh matn. Ishlatilgan: chap menu, `add-mini` (+), `msFilter` qatorlari, sidebar toggle chevron (CSS bilan aylanadi). Sahifa sarlavhalarida emoji **yo'q** — ikonkani chap menu ko'taradi.
- **Responsive (kichik ekranli kompyuter)**: ⚠️ ASOSIY — `.main { min-width: 0 }`. `.app.show` grid'ining `1fr` track'i sukut bo'yicha `min-width:auto`, ya'ni kontentdan kichik bo'lolmaydi → kanban/keng jadval **butun sahifani** gorizontal scroll qilardi (1024px: scrollWidth 1752 / clientWidth 1014). Endi keng kontent o'z idishida scroll bo'ladi (`.kanban`/`.tbl-wrap`/`.pln-cal`). Moslashtirish bloki **stylesheet OXIRIDA** (media query specificity qo'shmaydi → `.kan-col`/`.pln-grid` dan keyin turishi SHART) va `min-width:981px` chegarali (980/880px dagi mobil qoidalar buzilmasin): ≤1440 padding, 981–1280 (kan-col 252, pln-grid 48+7×96, dashSplit 1 ustun), 981–1180 (kan-col 236), ≤1180 pln-layout 1 ustun, `max-height:780px` balandlik tuzatishlari.
- **Qoralama / autosave (`dft*`)** *(2026-08-11)*: `// ============ 💾 QORALAMA / AUTOSAVE (dft*) ============` bloki, `gs*` dan bevosita OLDIN, `init()` dan **tashqarida** (banner tugmalari inline `onclick` — 2026-07-28 saboqi). Yozilgan matn forma yopilsa ham yo'qolmaydi.
  - **Faqat `text`/`textarea`**. Tanlagichlar (`ss*` search/msearch, `select`, `choice`, `toggle`, `datetime`, `number`, sana, fayl, subtask, `msSelected`) **ATAYLAB kirmaydi** — ular tashqi holatga bog'liq (a'zo o'chishi, bo'lim o'zgarishi) va 7 kundan keyin mavjud bo'lmagan id'ni tiklash xato manbai bo'lardi.
  - Kalit: `localStorage['tfdraft_<wsId>_<userId>_<name>']` → `{v:1, at, d:{...}}`. ⚠️ Workspace o'zgaruvchisi **`currentWs`** (`currentWorkspace` YO'Q). `DFT_DEBOUNCE=400ms`, `DFT_TTL=7 kun` (eskisi `dftLoad`da o'chiriladi), `DFT_MAX=20000` belgi (kesilsa `console.warn`).
  - 🔴 **Tozalash FAQAT HAQIQIY MUVAFFAQIYATDAN KEYIN** (6-qoida). `uiForm`ning `submit()` da `dftClear` **YO'Q** — `submit()` = "OK bosildi", saqlash emas: validatsiya va DB yozuvi chaqiruvchida, undan keyin turadi. Har chaqiruvchi throw'lardan keyin o'zi tozalaydi: `saveCreate`, `addComment`, `saveProject`, `prjRenameOrDelete`, `prjSaveTask`. Bekor/Escape/backdrop → qoralama **saqlanib qoladi** (`close()` faqat `dftDetach`).
  - **Kalitlar bitta joyda**: `DFT_K_PROJECT_NEW`, `dftKeyProject(id)`, `dftKeyPrjTask(projectId)`, `dftKeyComment(taskId)`, `dftCreateName(deptId)` — `draftKey` va `dftClear` harfma-harf bir xil bo'lishi shart, qo'lda satr yozilmaydi.
  - ⚠️ **`task:new` va `task:new:<deptId>` — ATAYLAB alohida kalit**: bo'lim ichidan ochilgan modal boshqa kontekst, umumiy qoralama u yerga tushmasin.
  - **Tiklash UI — `uiConfirm` EMAS** (modal ustiga modal chiqmasin): forma ichida `.dft-bar` banneri (`dftBarHtml`, kehribar, `⟲ Saqlanmagan qoralama bor (vaqt)` + Tiklash/O'chirish). Banner **faqat**: qoralama bor + forma tegilmagan (yaratishda bo'sh, tahrirda `opts.initial` bilan bir xil) + qoralama hozirgidan farq qiladi. Foydalanuvchi yoza boshlasa banner **yo'qoladi** (`dftOnInput`) — aks holda keyingi "Tiklash" yangi matnni bosib ketardi.
  - **Ulangan 3 joy**: (1) vazifa modali — `dftAttachCreate` `openCreateInternal` oxirida, kalit `_dftCreateName`; (2) izoh `#newCmtInput` — `dftAttachComment` `renderDetailBody` oxirida; (3) `uiForm(title,fields,{draftKey})` — berilmasa xatti-harakat **mutlaqo o'zgarmaydi**. Ishlatgan formalar: `openCreateProject`, `prjRenameOrDelete`, `prjAddTask`.
  - 🔴 **Ikki xotira/o'chirish tuzog'i** (tester tutgan): (a) `#cTitle`/`#cDesc` DOM'da **doimiy** → `input` listener `dataset.dftBound` bilan bir marta bog'lanadi va joriy ulanish `el._dftToken` orqali topiladi; modalni tez yopib-ochganda oldingi ulanishning kutayotgan debounce yozuvi **bekor qilinadi** (u bo'shatilgan maydonni o'qib qoralamani o'chirardi). (b) `renderDetailBody()` har safar **YANGI** `#newCmtInput` yaratadi (~25 chaqiruv joyi + realtime) → yangi elementda `_dftToken` yo'q, tozalash ishlamaydi va `_dftReg` cheksiz o'sardi; shuning uchun `_dftCmtTok` modul darajasida saqlanib, yangi `dftAttach` dan **oldin** `dftDetach` qilinadi.
  - `snapshot` — banner ochiq turganda foydalanuvchi yozib yuborsa ham "Tiklash" **aynan yuklangan** qoralamani qaytaradi. localStorage quota/JSON xatosi → `console.error` + buzilgan kalit o'chadi, ilova yiqilmaydi.
  - CSS `.dft-*` (moslashtirish blokidan OLDIN). ⚠️ Ilovada **dark theme YO'Q** (bitta `:root`) — `.dft-bar` rangi `#92400e` qattiq yozilgan; dark theme qo'shilsa shu blokka override kerak.
- **Universal qidiruv (`gs*`)**: `// ============ 🔍 UNIVERSAL QIDIRUV (gs*) ============` bloki, `init()`dan **oldin, global scope'da** (~14222). **Modal YO'Q** — topbardagi `#topSearchInp` haqiqiy input (`oninput=gsOnInput`, `onkeydown=gsKey`, `onfocus=gsOpen`), natijalar `#gsDrop`/`#gsList` panelida uning ostida ochiladi (`.gs-drop`, `@keyframes gsDrop` .22s). Panel **`<body>`ning bevosita farzandi** + `gsPosition()` (fixed, `getBoundingClientRect`) — topbar `sticky`/`z-index:20` bilan urishmasin; `z-index:90` (modal 100 ostida). ⌘K → `gsFocus()` (fokus + select + ochish). `gsClose()` matnni saqlaydi (Esc/tashqariga bosish, `gsWatch` mousedown), `gsDone()` natija tanlangandan keyin tozalaydi. Xodim qatorida **`＋ Vazifa`** tugmasi (`.gs-act` → `gsNewTaskFor(uid)`): `openCreate()` + `msSelected=[{type:'user'|'me',id,label}]` + `msUpdateSummary()`/`msFilter()`. Shu sababli natija elementi `<div role="option">` (button ichida button bo'lmasligi uchun). Yagona dvigatel: `gsLocal(q,qd)` (kesh: wsMembers/tasksCache/departments/branchesCache/positionsCache/projectsCache) + `gsServer(q)` (parallel `ilike`: tasks/projects/positions, har biri `{error}`) → `gsApplyServer` (id bo'yicha dublikatsiz) → `gsPaint()`. Guruhlar `GS_GROUPS`, guruhda 5 ta + `gsShowMore`. Telefon `gsPhoneMatch`/`gsHiPhone` (raqamlargacha normallashtirish, oxirgi 9 raqam). Highlight `gsHi`/`gsHiPhone` (escapeHtml ichida). Debounce `GS_DEBOUNCE`, eskirgan javob `_gsSeq` bilan bekor. CSS `.gs-*`; mobilda (`@media max-width:640px`) panel deyarli butun kenglik (`gsPosition` `innerWidth<=640`). `openCmdPalette`/`closeCmdPalette` — eski nom aliaslari (⌘K listener va init'dagi Escape ishlatadi; Escape `_gsShown`ga qaraydi). Tezlik uchun ixtiyoriy `TASKFIX_SEARCH.sql` (pg_trgm GIN).
  - ⚠️ **Saboq (2026-07-28)**: eski qidiruv butunlay `init()` ICHIDA edi → funksiyalar lokal, HTML inline handler esa global scope'da baholanadi → `openCmdPalette is not defined`. **Inline handler chaqiradigan funksiya global bo'lishi SHART** (top-level `function`). Top-level `const`/`let` (masalan `$`) inline handler'ga ko'rinadi, funksiya ichidagilar YO'Q.
- **Import**: `hrImport*` (preflight 5b — telefon+ism blokeri mijozda).
- **EF**: `admin-import-staff` (phase: identity | photos | connect), `sync-provodka-kassa` (Harajat kassa → Provodka RPC), `admin-create-employee` (v4 — xodim yaratish/topish + a'zolik + lavozim/filial + parol emaili; **repoda tiklandi 2026-07-23**, deploy kutilmoqda). Boshqa EF manbalari repoda YO'Q (send-email, tg-send, tg-webhook... deployed).
- **Loyiha (Project)**: `// ============ LOYIHALAR ============` bloki (~2620–3000) + flow yordamchilari (`prjShiftNextDeadline`/`prjReloadTasks`). Turi `projects.flow_type` (`sequential`|`parallel`, default sequential) — **faqat yangi bosqich uchun default** (`prjAddTask` toggle'i), har vazifa o'z `depends_on_prev`ini saqlaydi. Qulf: `tasks.is_locked` (server trigger **avtoritet**) — `prjTaskOpen` avval `is_locked`ka qaraydi, `prjShouldBeOpen` faqat mantiqni hisoblaydi (ikkisi ziddiyatda = "qulf qotib qolgan" → owner'ga `prjUnlockTask` tugmasi). Boshqa: `prjRenderHero` (kim ushlab turibdi), `prjMoveTask` (↑↓ tartib), `prjSyncLocks`, `prjApplyTypeToTasks`, `prjRenderHeader`, `prjShowError(msg, sub?)` (2-argument ixtiyoriy tushuntirish). CSS `.prj-*`.
  - **Assignee loyihani ko'radi (`TASKFIX_LOYIHA_RLS.sql`, 2026-08-11)**: bosqichi menga biriktirilgan loyihani to'liq ko'raman. Server tomonda: `can_see_project(project,user)` (**SECURITY DEFINER** — `tasks` RLS'ini chetlab o'tadi, shuning uchun `projects` policy'sidan chaqirilganda **42P17 rekursiya bo'lmaydi**; `anon`/`authenticated` dan REVOKE + faqat `GRANT EXECUTE authenticated`; tanasida **`p_user = (SELECT auth.uid())` MAJBURIY** — funksiya PostgREST `/rpc/can_see_project` orqali ham chaqiriladi, busiz begona odam haqida ma'lumot berardi) + `projects_select_assignee` + (kerak bo'lsa) `tasks_select_project_member` + (workspace_id ustuni bo'lsa) `project_members_select_project_view`. Mavjud policy'lar **tegilmaydi** — yangi PERMISSIVE policy OR bilan qo'shiladi. Mijozda **hech qanday to'siq ochilmadi**: ko'rish avvaldan ochiq edi (`projects`/`project` `managerOnlyPages` da EMAS, sidebar havolasi har a'zoda), yozuv esa hamon `isOwnerLike()` (`prjAddTask`, `prjRenameOrDelete`, `prjDeleteProject`, `prjMoveTask`, `prjUnlockTask`, `prjInviteMember`, takrorlanish reseti).
  - 🔴 **`_prjDenied` — loyiha menga ochiq emas** (`loadProjectDetail` qator qaytarmadi = RLS): bayroq `true` bo'lganda `refreshActiveView()` ning `project` shoxi, `prjRenderHero`, `renderProjectFlow/Kanban/Members` **QAYTA CHIZMAYDI**. Busiz fon sahifalash/realtime chaqirgan `refreshActiveView()` "Bu loyiha sizga ochiq emas" blokini o'chirib, o'rniga YOLG'ON "Bosqichlar yo'q — administrator qo'shgach ko'rinadi" xabarini qoldirardi. Bayroq `openProject` va `loadProjectDetail` boshida tozalanadi, blokdan (`prjShowError`) **OLDIN** qo'yiladi. Xabar ikki xil: adminga SQL fayl nomi aytiladi, oddiy a'zoga yo'q.
  - **Muddat + 4 HOLAT (`prjState`)** *(2026-08-11, A-bosqich, BRIEF_TASKFIX_LOYIHA.md 2-band)*: `TASKFIX_LOYIHA.sql` (**RUN kutilmoqda**) — `projects` ga `start_at`/`end_at` (timestamptz), `recur_type` (TEXT+CHECK `none|daily|weekly|monthly`), `recur_parent_id` (o'z-o'ziga FK, `ON DELETE SET NULL`) + CHECK `projects_dates_chk (end_at > start_at)` + indeks `idx_projects_ws_start`. `recur_type`/`recur_parent_id` **zaxira** — mijoz hozircha YOZMAYDI (B-bosqich: recursive + tarix).
    - 🔴 **4 holat SAQLANMAYDI — hisoblanadi** (`prjState(p, tasks)` → `upcoming|active|ended|overdue|paused`, `PRJ_STATE` xaritasi + `prjStateBadge`). **`projects.status` ustuniga TEGILMAGAN** (u qo'lda hayot sikli `active|paused|done`, "Sozlash" oynasi yozadi): (1) uni bosib ketsak "pauza" va mavjud ma'lumot buzilardi; (2) `overdue` devor soati bilan o'zgaradi — saqlangan ustun eskirar va cron talab qilardi. Tartib: `status='paused'` → `paused`; `status='done'` → `ended`; `now < start_at` → `upcoming`; `now > end_at` → bosqichlar hammasi `completed/cancelled` bo'lsa `ended`, aks holda `overdue`; qolgani `active`. ⚠️ `tasks` **berilmasa** (`null` — masalan bosqich so'rovi xato bergan, `_prjTasksErr`) yoki **bo'sh** bo'lsa → `overdue` (bo'sh ro'yxat "tugagan" emas: karta pastda "Bosqich qo'shilmagan" deb turib tepada "Tugagan" deyishi ziddiyat edi).
    - Ranglar (CSS `.prj-state.upcoming/.active/.ended/.overdue/.paused`, mavjud bosqich modifikatorlari `.done/.cur/.lock/.open/.cancel` bilan to'qnashmaydi): ko'k-kulrang `#64748b` · yashil `#16a34a` · kulrang `#6b7280` · qizil `#dc2626` · kehribar `#d97706`. `.prj-range` — `icon('calendar')` + `prjFmtDT` (dd.mm.yyyy HH:MM). Eski `prjStatusBadge` **o'chirildi** (prjState bilan ziddiyatli belgi chizardi); `prjStatusKey` KERAK — qolgan.
    - Sana: `uiForm` `datetime` → `prjDtToIso` (`+05:00` = Toshkent, `prjAddTask` naqshi, `isNaN` qalqoni bilan — busiz 5–6 xonali yil `toISOString()` da throw qilib "unhandled rejection" berardi), teskarisi `prjDtInput` (`toInputDt`). Validatsiya: `end <= start` → toast, saqlanmaydi (DB CHECK — ikkinchi to'siq).
    - 🔴 **Ustun yo'q fallback'i SIKL bilan** (`prjRunWithColFallback(obj, run)`): PostgREST bitta xatoda **bitta** ustun nomini qaytaradi, prod'da esa `flow_type` HAM, `start_at` HAM yo'q bo'lishi mumkin — ketma-ket ikki `if` bo'lsa natija qaysi ustun birinchi aytilishiga bog'liq bo'lib qolardi (loyiha umuman yaratilmasdi). Sikl 3 martagacha: yo'q ustunni `row`/`patch` dan o'chirib qayta yuboradi; boshqa xato (RLS) → darrov `break`. Aniqlash `prjNoLoyihaCol` / `prjNoFlowTypeCol`. ⚠️ Tashlangan maydon **jimgina yo'qolmaydi**: `prjOkMsg` muvaffaqiyat xabariga sababni qo'shadi (`✅ Loyiha yaratildi (⚠️ sana saqlanmadi — TASKFIX_LOYIHA.sql ...)`) — **har safar**, `_prjLoyihaWarned`/`_prjFlowWarned` faqat `console.error` ni bir martaga cheklaydi. Patch butunlay bo'shab qolsa → "Saqlanmadi: sabab" (jim qaytmaydi). Bayroqlar `loadCurrentContext` da tozalanadi (SQL sessiya davomida run qilinsa F5 shart emas).
    - `prjRenameOrDelete` update'i endi **`.select('id')` qiladi** va qator sonini tekshiradi — RLS jimgina to'sganda "✅ Saqlandi" yolg'on chiqmasin.
  - **Takrorlanish + tarix filtri** *(2026-08-11, B-bosqich)*: `recur_type` (`none|daily|weekly|monthly`) **eski `recurrence_days`/`recurrence_weekday` bilan YONMA-YON** — `prjRecurType(p)` 'none' bo'lmasa YANGI mantiq, aks holda eski (o'zgarishsiz). Yordamchilar: `PRJ_RECUR`, `prjRecurOptions/Chip/Label`, `prjPeriodStart(ms,type)` (davr boshi, hafta **dushanbadan**, Toshkent = `+PRJ_TZ_OFF` va `getUTC*`), **`prjAddPeriod(iso,type)`** (⚠️ oy arifmetikasi: 31-yanvar+1oy = 28/29-fevral, `new Date(y,m+1,31)` sakrashi YO'Q), `prjNextDates(p,type)` (o'tmishda qolgan sanani kelajakka suradi, **ikkovi bo'lsa davomiylik saqlanadi** — oy qisqarishi DB CHECK `end>start` ni buzmasin). ⚠️ `daily`/`weekly` — teng davr, shuning uchun **sikl emas, arifmetik sakrash** (bo'lish bilan; avval 500 qadamli qorovulga urilib, 500 kundan uzoq ochilmagan loyiha sanalari abadiy o'tmishda qolardi); `monthly` esa sikl (oy uzunligi o'zgaruvchan) + `PRJ_SHIFT_MAX=1200` (~100 yil).
    - 🔴 **Optimistik qulf (`prjResetRecurrence`)**: avval **DA'VO** — `update({last_reset_at:now}).eq('id').eq('last_reset_at', eski)` (NULL uchun **`.is(col,null)`**, `.eq(col,null)` PostgREST'da ishlamaydi) + `.select('id')`; 0 qator = boshqa admin oldi → **jim chiqish** (toast YO'Q, xato emas). Da'vodan keyingi xato **qayta urinilmaydi** — ataylab: "ikki marta ochilgan"dan ko'ra "bir sikl o'tkazilgan" afzal. Reset: bosqichlar `new` + `bajardi_*` (yo'q bo'lsa `prjNoV8Col` bilan ularsiz qayta), loyiha sanalari keyingi davrga, bosqich deadline'lari **o'sha delta**ga (`PRJ_DL_CHUNK=10` talik bo'laklarda `Promise.all` — 20 bosqichli loyihada ochilish 20 round-trip'ga qotib qolmasin; har javobda `{error}` + qator soni), `prjSyncLocks`, `tasksCache` sinxron, `thRecordMany` (tarix). `await` paytida boshqa loyihaga o'tilsa mahalliy qadamlar `local=[]` bilan o'tkazib yuboriladi.
    - 🔴 **`status` `paused` YOKI `done` → hech qachon takrorlanmaydi** (ikkala mantiq uchun, `prjRecurDue` boshida). `done` ham to'siladi, chunki `prjState` uni `ended` deb hisoblaydi va u "Tarix" filtrida turadi — reset bo'lsa bosqichlari `new` ga qaytib, sanalari siljib ketardi-yu, belgisi "Tugagan" bo'lib qolaverardi (takrorlanish va tarix moduli bir-biriga zid bo'lardi). Qayta yoqish: holatni "Faol" ga qaytarish.
    - 🔴 **Takrorlanish yoqilganda `last_reset_at` DOIM hozirgi vaqtga yoziladi** (`prjRenameOrDelete` oxiridagi `legacy` so'rovi, eski qiymat bor-yo'qligiga qaramay). `if (!p.last_reset_at)` sharti bilan bo'lganda eski takrorlanish ishlatgan loyihada (u yerda `prjEditRecurrence` `last_reset_at` ni **doim** to'ldiradi) "Har oy" tanlangan zahoti `prjRecurDue` true bo'lib, loyiha **darrov va jimgina** reset bo'lardi — bajarilgan bosqichlar `new` ga qaytardi. Bu aynan eski→yangi migratsiya yo'li edi.
    - `prjResetRecurrence` `loadProjectDetail` dan **`silent=false`** bilan chaqiriladi (6-qoida) — bu fon jarayoni emas, foydalanuvchi loyihani ochib kutib turibdi; yarim bajarilgan sikl (deadline surilmadi / sana saqlanmadi / RLS to'sdi) jimgina yutilmaydi. Da'vo mag'lubiyati baribir jim (normal holat).
    - UI: "Sozlash" oynasida `choice` maydoni (4 karta); eski sozlama faol bo'lsa hint aytadi; yangi tur yoqilganda eski `recurrence_*` **alohida so'rovda** tozalanadi (`recur_type` ustuni yo'q bo'lsa eski jimgina o'chmasin). `prjRunWithColFallback` endi **faqat xatoda ATALGAN** ustunni tashlaydi (`prjNamedLoyihaCols`, 5 urinish) — yarim migratsiya qilingan DB da (sana BOR, `recur_type` YO'Q) sana tahriri behuda yo'qolmasin; `droppedRecur` + `PRJ_MSG_NO_RECUR`.
    - **Tarix filtri (`prjf*`)**: loyihalar ro'yxatida 3 chip — Faol (default, `ended` yashirin) / Tarix / Hammasi; `prjState` dan hisoblanadi, `localStorage['prj_filter_<ws>']`, `prjfLoad()` `loadCurrentContext` da. CSS **`.prjf-*`** (`.plf-*` bosilmaydi). ⚠️ Takrorlanuvchi loyiha **joyida** reset bo'ladi (nusxa yo'q) → har sikl uchun alohida tarix yozuvi YO'Q (ataylab: nusxalash `tasks` ni shishirardi). Yangi ikonkalar: `repeat`, `archive`.
  - **🔴 Boshlanish darvozasi (`taskHiddenByStart`)** *(2026-08-12)*: `start_at` kelmaguncha loyiha bosqichi bajaruvchiga **ko'rinmaydi va xabar ketmaydi**; kelgach sequential (navbat) / parallel (birdan) — eski mantiq o'zgarmagan.
    - 🔴 **FAQAT KO'RSATISH DARVOZASI — DB'ga hech narsa yozilmaydi.** `taskHiddenByStart(t)` → `prjInfoById(project_id)` (`_curProject` → `projectsCache` → yengil kesh `prjStartMap`) → `prjNotStarted(p)` (`start_at` bor VA `Date.now() < start_at`). `completed/cancelled` **istisno**.
    - ⚠️ **`is_locked` ga YOZILMAYDI — u sof KETMA-KETLIK belgisi** (server trigger avtoriteti). Boshlanishni qulfga yozib qo'yilsa `start_at` o'tgach vazifa **o'zi ochilmasdi**: qulfni odam ochishi kerak bo'lardi, 1-sequential bosqichni esa trigger **hech qachon** ochmaydi (oldingisi yo'q). Darvoza `Date.now()` ga qaragani uchun vaqt o'tishi bilan **avtomat** ochiladi — cron ham, "ochish" amali ham kerak emas. `prjShouldBeOpen`/`prjTaskOpen` darvozani hisobga oladi (karta "Loyiha 12.08.2026 da boshlanadi" deydi, `stuck`/"Qulfni ochish" chiqmaydi), lekin **`prjSyncLocks` da qorovul bor** — boshlanmagan bosqichga qulf yozmaydi.
    - **Ulangan filtrlar**: `updateDashboard` · `applyTaskScope` (ro'yxat/kanban/jadval) · `renderTasksCalendar` (avval `applyTaskScope` dan UMUMAN o'tmasdi — eski `is_locked` teshigi ham yopildi) · `deptTasks()` (u yerda ham `is_locked` filtri yo'q edi; `moveTaskToDept` bosqichni bo'limga ko'chira oladi) · `plnSideTasks`. **Tegilmagan** (eski teshik, alohida ish): statistika/hisobot (`tasksCache` to'g'ridan) va universal qidiruv `gs*`.
    - **Kesh**: `loadProjectStarts()` — `loadCurrentContext` 1-to'lqinida `select('id,start_at,status')`, throw qilmaydi, "ws almashdimi" tekshiruvi bor. Busiz darvoza dashboard/planner'da ishlamasdi (`projectsCache` faqat "Loyihalar" sahifasida to'ladi). Loyiha qatori ko'rinmasa (RLS) darvoza **hech narsa yashirmaydi** → `TASKFIX_LOYIHA_RLS.sql` ham kerak.
    - **Kechiktirilgan xabar**: `prjStartKickoff(project)` (LAZY, `loadProjectDetail` da, faqat `isOwnerLike`) — **optimistik da'vo** `update({start_notified_at}).is('start_notified_at',null).select('id')`, 0 qator = jim (+`console.warn`: RLS ham shunday ko'rinadi). ⚠️ Nishon `prjStartNotifyTarget(t,p)`: **`created_at < start_at`** — ya'ni xabari yaratilishida to'silganlar. Busiz "bugun boshlangan" loyihaga bosqich qo'shilsa `prjSaveTask` xabar yuborardi, keyin kickoff **ikkinchi marta** yuborardi. Ustun yo'q → xabar yo'q (ko'rinishga ta'siri yo'q), sabab bir marta console'ga (`prjNoStartCol`).
    - To'sish nuqtalari: `prjSaveTask` (xabar), `tblUpdate` (assignee), `updateField` → `tgNotifyTaskEdit` (nom/muddat tahriri ham sizib chiqardi).
    - `prjRenderHero` va `prjcCardHtml` ga **"boshlanmagan" shoxi** qo'shildi — busiz ikkalasi `open.length===0` ni ko'rib "Barcha bosqichlar tugadi" deb **yolg'on** yozardi.
  - **🔔 "Loyihaga taklif" in-app bildirishnomasi (`notifyProjectAssign`)** *(2026-08-12)*: loyiha bosqichi kimgadir biriktirilganda qo'ng'iroq panelida xabar chiqadi. **Yagona choke-point** — `notifications` ga faqat shu funksiyadan yoziladi (`notifyMention` naqshi, fon amali: toast YO'Q, xato `console.error`).
    - **Yangi SQL KERAK EMAS**: `notifyMention` allaqachon boshqa odam nomiga insert qiladi → INSERT policy mavjud. `TASKFIX_NOTIF_CHECK.sql` — **faqat SELECT + RAISE NOTICE** diagnostikasi (ustunlar, RLS holati, policy matnlari); ishga tushirish ixtiyoriy, hech narsani o'zgartirmaydi.
    - ⚠️ `kind:'task_assigned'` — **MAVJUD tur** (`NOTIF_ICONS` da ikonkasi bor). Yangi kind o'ylab topilmaydi — noma'lum kind ikonkasiz qolardi.
    - ⚠️ `meta.task_id` + `meta.workspace_id` **MAJBURIY** — bosilish (`openNotifTask`: ws almashtirish + `openDetail`) aynan shu ikki kalitga qarab ishlaydi, qo'shimcha kod kerak emas. Qolgan meta: `project_id`, `project_name`, `invited_by_name`.
    - 🔴 **Faqat loyiha bosqichi** (`task.project_id`) — oddiy vazifada matn mos kelmaydi, jimgina chiqib ketadi. O'ziga o'zi yubormaydi.
    - 🔴 **Boshlanish darvozasi ichida**: `prjNotStarted(proj)` bo'lsa yozilmaydi (bosqich egaga ko'rinmaydi ham); xabar loyiha boshlanganda `prjStartKickoff` dan **bir marta** ketadi — takrorni `prjStartNotifyTarget` (`created_at < start_at`) qorovuli to'sadi, ya'ni `prjSaveTask` allaqachon xabar bergan bosqich kickoff ro'yxatiga tushmaydi. `completed/cancelled` istisno (`taskHiddenByStart` bilan bir xil).
    - **4 ulanish**: `prjSaveTask`, `tblUpdate` (assigned_to), `updateField` (assigned_to — u yerda **faqat in-app** qo'shildi, email/tg mantiqiga tegilmadi), `prjStartKickoff`.
    - ⚠️ `prjStartMap` keshiga **`name` qo'shildi** (`loadProjectStarts` endi `id,name,start_at,status` o'qiydi): busiz Vazifalar sahifasidan biriktirilganda (`projectsCache` bo'sh) sarlavha "Loyihaga taklif qilindingiz: <nom>" o'rniga umumiy "Sizga vazifa biriktirildi" ga tushib qolardi. `prjStartSync` mavjud `name`ni bo'sh qiymat bilan **bosmaydi**.
  - **Vizual: bosqich kartasi (`prjTaskCardHtml`) + loyiha kartasi (`prjc*`)** *(2026-08-11, BRIEF 5–6-band)*: ikkalasi **bir uslub tilida**. Holat rangi — chap chekkada 3px stripe, `box-shadow: inset 3px 0 0 var(--st|--pst)` bilan (`::before` EMAS — inset radiusga bo'ysunadi, `overflow:hidden` kerak emas; hover'da inset **qayta yoziladi**, aks holda chiziq yo'qolardi). ⚠️ Qulflangan/tugagan karta `opacity` bilan **so'ldirilmaydi** — matn o'qilmasdi; kulrang fon + uzuq chegara + ikonka bilan farqlanadi. Bosqich kartasida qabul qiluvchining **ko'rinadigan** yorlig'i bor (`.prj-asg-cap`) — ikkala shaxs bir xil avatar+ism ko'rinishida edi, sensorli ekranda tooltip yo'q. Emoji → `ICON_PATHS` (yangi: `user/clock/lock/unlock/link/play/x`); `PRJ_TYPES[k].icon` emojisi **faqat** `uiForm` `choice` kartalarida qoldi (`prjTypeOptions`), `prjTypeChip` SVG chiqaradi. Loyihalar ro'yxati — `.prjc-*` prefiksi (`.prj-*` band), grid `repeat(auto-fill, minmax(min(320px,100%), 1fr))` **media query'siz**; kartada progress bar + avatar stack (`PRJC_STACK_MAX=4` + "+N"). 🔴 **Progress ta'rifi ikkala ekranda BIR XIL**: bajarilgan = `completed` + `cancelled` (`prjcCardHtml` va `prjRenderHero`) — avval hero faqat `completed` sanardi va bekor qilingan bosqichi bor loyihada ro'yxat 100%, detal 50% ko'rsatardi; bu `prjState` ning "hammasi tugagan → ended" mantiqiga ham mos. Eski `.prj-grid/.prj-card/.prj-now*` CSS **o'chirilmadi** (kichik-ekran bloki ularga murojaat qiladi), lekin endi hech qayerda chizilmaydi.
  - **Refresh qoidasi**: vazifani o'zgartiruvchi HAR amal `tasksCache` VA `prjTasks` ni yangilashi shart (`refreshActiveView` faqat tasksCache'dan sinxronlaydi — o'chgan qator u yerda yo'q, shuning uchun o'zi tozalanmaydi). O'chirish/ko'chirishdan keyin `loadProjectDetail()` yoki `prjReloadTasks()` (u endi serverda yo'q bosqichlarni ham tozalaydi). Mavjud bo'lmagan vazifa uchun `taskGoneLocal(id)` — ro'yxatni tozalaydi, xom "topilmadi" xatosi o'rniga yumshoq xabar; `isRowGone(err)` PGRST116'ni aniqlaydi.
- **☾ Namoz vaqtlari (`nmz*`)** *(2026-08-08, yang. 2026-08-09, BRIEF_TASKFIX_NAMOZ.md)*: Planner'dagi **"Muslim"** toggle. Sozlama **SHAXSIY** — `profiles.namoz_enabled` + `namoz_region` (TEXT+CHECK, 14 qiymat) + **`namoz_lat/namoz_lon`** (aniq joylashuv, ikkovi birga yoki ikkovi NULL), workspace'ga bog'liq emas. `TASKFIX_NAMOZ.sql` (**RUN kutilmoqda**; 2026-08-09 da kengaydi — **qayta run xavfsiz va kerak**) + `public.namoz_times` kunlik kesh (PK **`(region, geo, date)`**, CHECK: HH:MM, o'sish tartibi, viloyat, geo formati).
  - **🔴 Shahar aniqligi**: viloyat markazi 1–2 daqiqa xato berardi (Qarshi ≠ Qashqadaryo markazi). Endi geolokatsiyaning **aniq koordinatasi** saqlanadi va aladhan'ga aynan u yuboriladi; viloyat faqat **ko'rsatish + islomapi** uchun (islomapi koordinata qabul qilmaydi). Kesh kaliti `geo` = `''` (viloyat markazi) yoki `'38.85,65.80'` (0.05° ≈ 5.5 km) — busiz bir odamning shahar vaqti butun viloyatga tarqalardi.
  - **Manba tartibi aniqlikka qarab**: koordinata bor → aladhan birinchi (shahar aniq), islomapi zaxira; koordinata yo'q → islomapi birinchi (rasmiy), aladhan zaxira.
  - **Blok**: `NMZ_LAG=10` + `NMZ_DUR=25` — namoz vaqtidan 10 daqiqa keyin boshlanadi, 25 daqiqa (peshin 12:07 → 12:17–12:42). Blokda blok vaqti, tooltip'da namoz vaqti ham bor.
  - Planner setkasida **"Bajarilganlarni yashirish" tugmasi kalendar tabida yashirin** (`plnDoneToggle`, `plnSetTab`) — u yerda vazifa ro'yxati emas, vaqt bloklari; qolgan tablarda o'z joyida.
  - **Aniqlash**: `profiles.select('*')` da `namoz_enabled` **kalitining yo'qligi** = ustun yo'q (`_nmzMissing`) — `false` qiymatdan farqi shu. Modul o'chiq turadi, toggle sababni aytadi, planner ishlayveradi.
  - **Manbalar**: `islomapi.uz` (rasmiy, hanafiy) → yiqilsa `api.aladhan.com` (`method=3&school=1`, CORS `*`). 2026-08-08 da islomapi **502** edi, aladhan jonli tekshirildi (31/31 kun). ⚠️ **CORS uchun n8n proxy kerak bo'lsa — `nmzFetchIslom` dagi YAGONA URL almashtiriladi**. Ishlatilgan manba UI'da ochiq yoziladi (zaxira bo'lsa "1–3 daqiqa farq" ogohlantirishi bilan).
  - **Kesh 3 bosqich**: xotira (`_nmzDays`) → `namoz_times` (DB) → API (**oylik**, bitta so'rov). ⚠️ Keshga yozish har `authenticated` uchun ochiq, shuning uchun **har qator `nmzValidTimes()` dan o'tkaziladi** (format + qat'iy o'sish tartibi) — SQL CHECK'lari birinchi to'siq, bu ikkinchisi.
  - **Bloklar**: 5 namoz × 30 daq (`NMZ_DUR`), quyosh chiqishi **blok emas**. 🔴 `pointer-events:none` + `z-index:1` va `plnGetSchedule()` ga **yozilmaydi** → eski planner mexanikasi (surish/o'lchash/yaratish) buzilmaydi. Setka `PLN_H_START=7` dan boshlangani uchun bomdod (~03:47) ustun tepasida ingichka **"erta" chipi** bo'lib chiqadi (30 daqiqalik blok bo'lib ko'rinmaydi).
  - **Panel + countdown**: `nmzRenderPanel`/`nmzTickStart` (1s). "Hozir" **Asia/Tashkent** bo'yicha (`nmzNowMin`/`nmzTodayStr`) — qurilma soati boshqa mintaqada bo'lsa ham to'g'ri. Xufton o'tgach "Ertangi Bomdodgacha". ⚠️ Interval bitta, planner'dan chiqilsa/tab almashsa/toggle o'chsa **to'xtaydi** (`goPage`, `plnSetTab`, `nmzAfterChange`).
  - Viloyat: `navigator.geolocation` (faqat viloyat yo'q bo'lganda, 8s chegara) → haversine bilan taxmin → `uiForm` `search` bilan tasdiq (7-qoida). Ruxsat berilmasa bloklamaydi.
- **Provodka integratsiyasi**: `hk*` funksiyalar (`hkSync`/`hkTableToggle`/`hkSetDb`). Jamoa jadval 💵 ustuni + hodim detali checkbox. EF `sync-provodka-kassa` env: `PROVODKA_URL`, `PROVODKA_SERVICE_KEY`.

## Ochiq masalalar
- **Dublikatlar**: Akobir tasdiqlangan (`cleanup_duplicate_staff.sql`da qo'lda juftlik). ~14 haqiqiy odam, qolgan 126 sintetik. Qo'shimcha juftliklar `dup_pairs` `manual` ro'yxatiga qo'lda qo'shiladi.
- **EF deploy kutilmoqda**: `admin-import-staff` v3.1 (connect action), `sync-provodka-kassa` v1 (+ 2 env secret: PROVODKA_URL, PROVODKA_SERVICE_KEY).
- **SQL kutilmoqda**: 43, 45, 47, `TASKFIX_V8.sql`, `TASKFIX_PROJECT.sql`, `TASKFIX_HISTORY.sql` (vazifa tarixi — busiz "Tarix" bo'limi "yoqilmagan" deb yozadi), `TASKFIX_SEARCH.sql` (ixtiyoriy — qidiruv indeksi, ilova busiz ham ishlaydi), `TASKFIX_EMAIL_SETTINGS.sql` (email toggle'lari — busiz ilova ishlaydi, lekin sozlama saqlanmaydi va hamma email yoniq qoladi), **`TASKFIX_HR.sql`** (HR Service + Org Schema: `hr_settings`, `org_folders`, `org_people` + sikl triggerlari; `departments` ga TEGMAYDI — **busiz HR Service yoqilmaydi**, karta buni ochiq aytadi, qolgan ilova ishlayveradi), **`TASKFIX_NAMOZ.sql`** (namoz vaqtlari — busiz "Muslim" toggle yoqilmaydi, planner o'zgarishsiz ishlaydi; 2026-08-09 da koordinata + `geo` qo'shildi — **qayta run kerak**), **`TASKFIX_ORGCHART.sql`** (org chart: `node_x/node_y` — busiz kartani surish saqlanmaydi, ilova buni toast bilan aytadi), **`TASKFIX_EMAIL_SYNC.sql`** (profiles.email backfill + auth trigger — busiz eski xodimlarning emaili bo'sh ko'rinaveradi, faqat o'zi kirgan foydalanuvchi mijoz zaxirasi bilan tuzaladi), **`TASKFIX_LOYIHA.sql`** (loyiha muddati: `start_at`/`end_at`/`recur_type`/`recur_parent_id` — busiz loyiha yaratish/sozlash ISHLAYDI, lekin sana saqlanmaydi va buni har safar toast aytadi; 4 holat mijozda hisoblanadi, `projects.status` ga TEGMAYDI), **`TASKFIX_LOYIHA_RLS.sql`** (assignee loyihani ko'rsin: `can_see_project()` + 1–3 permissive SELECT policy — busiz bosqichi biriktirilgan hodim loyihani ochganda "Bu loyiha sizga ochiq emas" bloki chiqadi, qolgan ilova o'zgarishsiz; skript o'zi diagnostika qiladi, `tasks`/`project_members` policy'larini **faqat kerak bo'lsa** qo'shadi va oldin/keyin ko'rinadigan qatorlarni sanab tekshiradi — farq bo'lsa hammasi qaytadi. ⚠️ **RUN paytida `projects`/`tasks` ga ACCESS EXCLUSIVE qulf** tushadi — kam trafik vaqtida ishga tushirilsin; tranzaksiya REPEATABLE READ), **`TASKFIX_LOYIHA_START.sql`** (loyiha boshlanish xabari: `projects.start_notified_at` + qisman indeks + **backfill** — boshlangan loyihalar "xabari yuborilgan" deb belgilanadi, busiz admin eski loyihani ochganda takroriy xabar ketardi. **RUN TARTIBI: `TASKFIX_LOYIHA.sql` → `TASKFIX_LOYIHA_RLS.sql` → `TASKFIX_LOYIHA_START.sql`**. Busiz bosqichlarni yashirish/ko'rsatish TO'LIQ ishlaydi — faqat "loyiha boshlandi" xabari yuborilmaydi), **`TASKFIX_VIEWS.sql`** (👁 ko'rish soni: `task_views` + `task_view_bump()` SECURITY DEFINER RPC — busiz nishon **umuman ko'rinmaydi** va qolgan ilova o'zgarishsiz ishlaydi (`_tvMissing` → jim). Yozuv faqat RPC orqali, INSERT/UPDATE policy YO'Q; skript oxirida `RAISE EXCEPTION` tekshiruvi RPC tanasidagi ikkala qorovulni **birlashgan naqsh** bilan majburlaydi va SELECT policy'ning `qual` ini ham o'qiydi — kimdir uni `USING(true)` bilan qayta yozsa begona ws a'zosi sonlarni o'qirdi. Boshqa fayllarga bog'liq emas, tartib muhim emas) (TaskFix); `PROVODKA_HODIM_KASSA.sql` (Provodka loyihasida).
- **i18n (uz/ru/en) — ISHLAB CHIQILMOQDA** *(2026-08-17)*: butun UI 3 tilga. Til tanlash allaqachon bor edi — **Profil** sahifasida (`pickLanguage`, `.lang-pick`, `profiles.language`, 21-migratsiya), lekin tarjima ulanmagan ("to'liq tarjima yaqin orada" toast). Reja: **Sozlamalar**ga ko'chiriladi, **3 til** (kirill olib tashlanadi — bazadagi `uz-cyril` `uz` deb o'qiladi). Inventarizatsiya: **2303 nomzod / 2109 noyob UI matni** (`console.*` 227 tasi kirmaydi). 🔴 **Asosiy qiyinchilik — konkatenatsiya**: faylda `${...}` atigi 8 ta, jumlalar `'matn ' + qiymat + ' matn'` bilan yig'iladi (**281 tugallanmagan bo'lak**) → i18n'da ular bitta kalitga birlashtirilib `{0}` placeholder bilan yoziladi, aks holda ru/en da so'z tartibi buziladi.
  - **HOLAT (2026-08-17): tarjima TUGADI, tasdiq kutilmoqda, ULANMAGAN.** `i18n.json` — **1824 kalit** `{kalit:{uz,ru,en}}` (`<modul>.<snake>`, 52 modul, 278 tasi placeholderli); `i18n_meta.json` — har kalit uchun `_src`/`_lines`/`_note` (ulash uchun). Hisobotlar: `I18N_MERGE_REPORT.md` (qamrov, ziddiyat), `I18N_FIX_REPORT.md` (sifat tozalash qarorlari). 2109 manba matnining hammasi hisobga olingan: 1824 kalit + 438 `skipped` (texnik satr — CSS klass, URL, localStorage kaliti, DB qiymati).
  - 🔴 **ULASHDA: almashtirish QATOR/KONTEKST bo'yicha, global matn replace EMAS.** `"ta"`, `"tanlandi"`, `"Tarix"` kabi bo'laklar bir nechta har xil kalitga tegishli. `_lines` chunk fayllaridan olingani uchun ~6 qatorga siljigan bo'lishi mumkin — matn + atrof bilan tasdiqlanadi.
  - 🔴 **TARJIMA QILINMAYDIGAN qiymatlar** (tekshiruvda tutilgan): `NMZ_REGIONS` dagi `value` (`profiles.namoz_region` CHECK qiymati) va `api` (islomapi URL parametri) — faqat `label`/`sub` tarjima qilinadi, bir qatorda `label === api` bo'lgani uchun xavfli; `aladhan.com`/`islomapi.uz` — `namoz_times.source` ustuniga yoziladi va **umumiy keshda** yashaydi; `<option value=…>`, `ROLE_DEPT` kalitlari, status kodlari.
  - Bir xil uz matni **7 joyda ataylab har xil tarjima** oladi (kontekst boshqa: `Bekor` tugma = «Отмена», status = «Отменена»; `Faol` buyruq = «Активна», loyiha = «Активные») — `I18N_FIX_REPORT.md` da ro'yxati.
- **Scale (2026-07-30, BRIEF_TASKFIX_SCALE.md)**: `TASKFIX_SCALE.sql` — **RUN kutilmoqda**. Bitta tranzaksiyada: (a) indekslar `idx_tasks_ws_created(ws,created_at DESC,id DESC)`, `idx_tasks_ws_status`, `idx_tasks_ws_deadline`, `idx_ws_members_user_ws`; (b) RLS: `pg_policies` dan o'qib `auth.uid()|jwt()|role()` → `(SELECT ...)` bilan o'rab `ALTER POLICY`. **Xavfsizlik**: o'zgarishdan oldin/keyin har foydalanuvchi × jadval bo'yicha ko'rinadigan qator sanaladi (`authenticated` roli ostida); bitta raqam farq qilsa `RAISE EXCEPTION` → DDL ham qaytadi. Asl policy matni `public.taskfix_rls_backup` da, qaytarish `TASKFIX_SCALE_ROLLBACK.sql`. Yakka `tasks(status)`/`tasks(deadline)` indekslari **ataylab qo'shilmadi** (so'rovlar doim `workspace_id` bilan keladi → kompozit yaxshiroq; `v_add_singles` bilan yoqiladi). Skript o'zi before/after `EXPLAIN ANALYZE` jadvalini chiqaradi.
- **Stress/perf test (2026-07-30, BRIEF_TASKFIX_STRESS.md)**: `STRESS_TEST_README.md` (tartib, natija, tuzatishlar). **1-test BAJARILDI**: boot 6079ms / 9 to'lqin / 2.1× — sekinlik ketma-ketlikda, DB'da emas (har so'rov 300–400ms). **Boot parallellashtirildi** (pastga qarang) → kutilgan 4 to'lqin. **2 va 3-test RUN kutilmoqda**: `stress_test.js` (10/50/100/500, faqat GET, `--yes` shart, xato>30% da bekor), `TASKFIX_STRESS_VOLUME.sql` + `TASKFIX_STRESS_CLEANUP.sql` (1k/10k/50k, alohida test ws `a57e5511-…`, Aros qora ro'yxatda). Hali ochiq: **`loadTasksFull()` LIMITSIZ** (owner uchun butun ws vazifalari) — indeks bilan hal bo'lmaydi, pagination kerak; 3-test shuni o'lchaydi.
- **Vazifalarni sahifalab yuklash (2026-07-30)**: `loadTasksFull()` endi LIMITSIZ emas. `TASKS_PAGE=500` — 1-sahifa darrov, qolgani **fonda** `tasksLoadRest()` bilan (`await` YO'Q), har sahifadan keyin `refreshActiveView()`. UX o'zgarmadi (foydalanuvchi baribir hammasini ko'radi), faqat 1-ekran tez. `tasksQuery(from,to)` — owner/a'zo farqi shu yerda. ⚠️ **ANCHOR**: 2-sahifadan boshlab `created_at <= _tasksAnchor` (1-sahifadagi eng yangi qator vaqti, SERVER qiymati) — busiz yuklash paytida yangi vazifa qo'shilsa offset siljib qator tushib qolardi. `_tasksOffset` alohida hisoblanadi (`tasksCache.length` EMAS — realtime uni siljitadi), qo'shishda id bo'yicha dublikat tekshiruvi. `TASKS_MAX=20000` chegarasi — yetilsa `console.error` + toast (jimgina kesilmaydi). Tartib `created_at DESC, id DESC` — `idx_tasks_ws_created` shuni qoplaydi. CSS `.tsk-chip`.
- **Boot yo'li (2026-07-30 optimizatsiya)**: `bootstrap()` — `auth.getSession()` (avval `getUser()` edi: har boot'da tarmoq; getSession localStorage'dan, token eskirgandagina tarmoqqa chiqadi) → `profiles` **∥** `loadWorkspaces()` (`Promise.all`). `loadCurrentContext()` — **2 to'lqin**: (1) 9 parallel (ws so'rovlari + `loadKanbanColumns` + `tg_bot_users(linked_user_id)`), (2) `profiles(a'zolar)` ∥ `tg_bot_users(integration)` ∥ `loadTasksFull()`. `loadHrData()` **fonda** (`.then` bilan, await YO'Q) — lavozim/rasm bezak, ilova ularni kutmaydi; tayyor bo'lgach `refreshActiveView`/`renderTeam`/`msFilter` jimgina yangilanadi. ⚠️ Fon bo'lgani uchun `loadHrData` ichida "ws almashdimi" tekshiruvi bor (eski natija yangisini bosmasin). `_myTgBotIds` 1-to'lqinda bir marta o'qiladi, `loadTasksFull` keshdan oladi (avval ikki marta so'ralardi).
- **Dublikat birlashtirish (2026-07-29, BRIEF_TASKFIX_MERGE.md + _2)**: `TASKFIX_MERGE_DIAG.sql` (1a — **faqat SELECT**) bajarildi. `TASKFIX_MERGE_1B.sql` (1b — **YOZADI**, RUN kutilmoqda): 2 juftlik qattiq yozilgan — Nodir KEEP `d7d8f5f3…` / REM `eb0d0a9e…` (6 vazifa kutiladi), Saidakbar KEEP `840a22ea…` / REM `50ef8131…` (12). Havolalar **dinamik** ko'chiriladi: merge paytida public+storage'dagi har uuid ustuni skanerlanadi (qo'lda ro'yxat YO'Q), UNIQUE indeks bo'yicha to'qnashadigan REMOVE qatorlari oldin o'chiriladi, `employee_details` esa maydon-maydon birlashtiriladi. Bitta tranzaksiya + oxirida RAISE tekshiruv (KEEP bor/REMOVE yo'q/havola qolmagan/vazifa soni). `auth.users` o'chirish faqat login qilmaganda va ichki EXCEPTION blokida (merge'ni qaytarmaydi). Rasm (Storage) SQL bilan ko'chmaydi — WARNING beradi. Universal emas: yangi dublikat chiqsa yangi diagnostika + yangi fayl.
- **Loyiha moduli (2026-07-27)**: (1) `TASKFIX_PROJECT.sql` — `projects.flow_type` + diagnostika (status CHECK, RLS, flow ustunlari). Ustun bo'lmasa ilova ishlaydi: `prjNoFlowTypeCol(err)` aniqlaydi, flow_type'siz qayta yozadi va ogohlantiradi. (2) `projects.status` — UI `active|paused|done` yozadi ("Sozlash"), DB'da CHECK bo'lsa xato chiqishi mumkin (SQL diagnostikasi shuni tekshiradi). (3) **Takrorlanish hamon LAZY** — reset faqat owner/admin loyihani ochganda (`prjRecurDue`+`prjResetRecurrence`); cron yo'q. (4) Loyiha RLS'i (34-migratsiya) repoda yo'q — server tomon tekshirilmagan, UI'da hamma o'zgartiruvchi amal `isOwnerLike()` bilan yopilgan.
- **V8 (2026-07-24)**: (1) "Bajardi: {ism} · sana" vazifa detalida — `tasks.bajardi_user_id`/`bajardi_at` (TASKFIX_V8.sql). `changeStatus` best-effort yozadi (ustunlar bo'lmasa jimgina console.error, zaxira `submitter_id`/`completed_at`). (2) Xodim filtri — `empFilter` (Set), `applyEmpFilter` (`tasks` + `department` sahifalari), yagona global filtr 2 bar bilan: 'tasks' (`#empFilterPanel`) va 'dept' (`#deptEmpFilterPanel`). localStorage `empFilter_<ws>`. Tasks: list/kanban/kalendar/jadval; Bo'lim detali: kanban/list/kalendar (Jamoa tabida bar yashirin). Planner'ga qo'shilmagan (yashirin filtrlanish oldini olish uchun `applyEmpFilter` faqat shu 2 sahifada). Funksiyalar scope-aware: `empFilterTogglePanel/RenderList(scope)`.

## Validatsiya buyrug'i
```bash
node -e "
const fs=require('fs'), vm=require('vm');
const h=fs.readFileSync('index.html','utf8');
const re=/<script(?![^>]*src=)[^>]*>([\s\S]*?)<\/script>/g;
let m, code=null;
while((m=re.exec(h))){ if(m[1].indexOf('async function init')!==-1) code=m[1]; }
try { new vm.Script(code); console.log('OK', code.length); }
catch(e){ console.log('XATO:', e.message); process.exit(1); }
"
```
