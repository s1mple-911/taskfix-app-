# CLAUDE.md — TaskFix loyihasi (doimiy xotira)

Har sessiyada BIRINCHI shu fayl o'qiladi. Har katta o'zgarishdan keyin **o'zing yangila**.

## Loyiha
- **TaskFix** — vazifa/jamoa boshqaruvi ilovasi. Supabase backend + bitta katta `index.html` (~560KB) frontend.
- **Supabase ref**: `nnpsbwsppgxbytlfloth`
- **Deploy**: GitHub Pages, **build yo'q** — `index.html` to'g'ridan xizmat qilinadi (vanilla JS, framework yo'q).
- **Til**: barcha UI matnlari toza **Uzbek Latin**.

## Qat'iy qoidalar (BUZMA)
1. Har `index.html` o'zgarishidan keyin **node-vm sintaksis validatsiya** (pastda). XATO chiqsa — to'xta, tuzat.
2. Yangi element ID'lariga **prefiks**: jadval `sch*`, routing `rt*`, rasm `ph*`, Org Schema `osc*`, HR Service `hrsv*`. Mavjud ID'larni buzma (`edtWd_*`, `edtWorkStart/End` empDtlSave'ga bog'liq). **Sahifa id'si/route'ini nomga qarab o'zgartirma** — masalan "Integratsiyalar" → "Qo'shimcha service" bo'ldi, lekin `page-integrations` va `#/integrations` O'ZGARMADI (havolalar buzilmasin).
3. SQL: **TEXT + CHECK, ENUM emas** (30/32-migratsiyalardagi saboq). Idempotent (`IF NOT EXISTS`), tekshiruvda `RAISE EXCEPTION` (jimgina o'tmasin).
4. `aros_staff_export.json` — **hech qachon commit qilinmaydi** (`.gitignore`da, 6MB).
5. Inline handler ichida arrow function **yo'q**; `escapeHtml()` har doim (XSS).
6. **Xato jimgina yutilmasin — muvaffaqiyat xabari faqat haqiqiy muvaffaqiyatda.** Bu eng ko'p buzilgan qoida (audit 2026-07-24: ~20 joy tuzatildi).
   - Har Supabase yozuv/o'qishda `const { error } = await ...` — supabase-js **throw QILMAYDI**, `{error}` qaytaradi. `await sb.from(...).update(...)` ni `{error}` siz yozsang, `catch` **hech qachon ishlamaydi** va DB rad etsa ham "Saqlandi" chiqadi. `if (error) throw error;` yoz.
   - `.functions.invoke(...)` ham `{error}` qaytaradi (funksiya-darajali xato throw emas) — uni ham tekshir.
   - `toast('Saqlandi'/'✅ ...')` faqat `throw`dan keyin, muvaffaqiyat yo'lida bo'lsin — yozuvdan **oldin** yoki tekshiruvsiz emas.
   - Bo'sh `catch(e){}` **taqiqlanadi**. Kamida `console.error(...)`. Foydalanuvchi kutayotgan amal bo'lsa — `toast`/`showMsg` ham. Fon bildirishnomasi (tg-send/email) bo'lsa — `console.error` yetarli.
   - Foydalanuvchiga ko'rsatiladigan xato **doim `translateErr(e.message || String(e))`** orqali (o'zbekcha; xom Postgres/RLS/tarmoq matni chiqmasin). Xom `e.message` ko'rsatma.
7. **Har qanday tanlash joyi QIDIRUVLI bo'lishi SHART.** Xom `<select>` bilan yangi dropdown qo'shilmaydi — yagona `ss*` komponenti ishlatiladi (`uiForm` maydon turi `search` yoki `msearch`). Multiselect **kontekstga qarab**: bir necha element tanlash mantiqan to'g'ri bo'lsa `msearch` (masalan loyihaga a'zo taklif qilish, papkaga bir necha xodim), bitta qiymat bo'lsa `search` (status, papka, bajaruvchi — `tasks.assigned_to` bitta ustun). Qidiruv **ikkalasida ham majburiy**. Batafsil: pastdagi `ss*` bo'limi.
8. RLS policy ichida `workspace_members` inline subquery **yozma** (rekursiya → 42P17). `is_ws_manager()`/`is_ws_member()` ishlat.
9. Mavjud helper: `uiForm(title,fields[{id,label,type,placeholder,hint}],{okText})→Promise<vals|null>`, `uiConfirm(title,msg,{danger,okText})`, `toast(text,'ok'|'err')`, `logActivity(action,{entityType,entityId,entityTitle,details})`, `$`, `escapeHtml`, `.hr-*`, `.ui-*`, `.xtbl`.
   - `uiForm` maydon turlari: `text|textarea|number|select|toggle|datetime|choice|search|msearch`. ⚠️ `select` — **eski**, yangi kodda ishlatilmaydi (7-qoida). **`choice`** — ikonli 2+ kartali radio: `options:[{value,label,icon,desc}]` (CSS `.ui-choice*`).

## DB holati
- **Migratsiyalar**: 38–44, 46 ishga tushgan (40 bo'sh o'tgan — 42 to'ldirgan). **45** (staff_phone_lookup), **43** (schedule_monthly), **47** (harajat_kassa) — foydalanuvchi ishga tushirishi kutilmoqda.
- Yangi migratsiya raqami: **48**dan.
- **Aros workspace**: `12b22aa6-dc45-4197-ae84-2e32e3cd56c2` — 126 hodim import qilingan, 80 rasm Storage'da (`employee-photos/{ws}/{uid}.jpg`, private bucket, signed URL bilan ko'rsatiladi).
- `legacy_id_map` + `staff_import_map` — **ikkalasi ham kerak** (har xil vazifa: legacy ID xaritasi vs import kuzatuvi).
- `employee_schedule_days`: `day_type IN ('on','off')`, PK (workspace_id, user_id, date), ~40k qator.

## Muhim modullar (index.html)
- **HR/Jamoa**: `loadHrData`, `renderEmployee`, `empDtlSave`, `renderTeamTable` (~9500–10800). `loadHrData()` endi **workspace ochilishida** ham chaqiriladi (`loadWorkspaceData`, org uchun) — lavozim butun ilovada bor bo'lishi uchun; u ichida `prefetchPhotoUrls` ham bor (avvalgi alohida photo_path so'rovi olib tashlandi).
- **Xodim tanlagichlarda lavozim**: `memberPos(uid)` (→ `teamMemberPos`, xatosiz bo'sh matn) va `memberNamePos(uid,name)` → `"Ism · Lavozim"`. **escapeHtml qilmaydi** — chaqiruvchida qilinadi. Ishlatilgan joylar: `msFilter` (avatar+ism+lavozim, qidiruv lavozim bo'yicha ham), `openCreate`→`cAcceptor`, `prjAddTask`, `prjInviteMember`, `empFilterRenderList` (+`.efo-pos`), `tblAsgRenderList`, `tblNewAsg`, `deptAddSel`, `cmtMentionInput/Render` (matnga qo'shiladigan `@Ism` o'zgarmadi), org chart nomzodlari.
- **Perf o'lchov (`pf*`)**: `init()` boshida `pfInit()`. **Sukut bo'yicha O'CHIQ** — `?perf=1` yoki `pfEnable()` bo'lmasa `fetch` o'ralmaydi (prodga ta'sir nol). Yoqilsa `window.fetch` o'raladi va Supabase so'rovlari yoziladi; `goPage()`/`bootstrap()` da `pfStart(label)` oyna ochadi, tarmoq `PF_IDLE=500ms` jim turganda yopiladi. Ko'rsatkichlar: so'rov soni, jami ms, eng sekin, **to'lqin** (ketma-ket bosqich soni — `pfWaves`, `PF_OVERLAP=5ms`: tegib turgan so'rovlar ustma-ust HISOBLANMAYDI) va parallellik (Σms/real ms). API: `pfReport()`, `pfDetail(n)` (ASCII waterfall), `pfExport()` (markdown→clipboard), `pfPanel()`, `pfClear()`, `pfDisable()`. Batafsil: `STRESS_TEST_README.md`.
- **Xodimni o'chirish (`rmx*`)**: `removeMember(uid)` — "o'chirish" = **workspace_members'dan chiqarish** (profil o'chmaydi!), shuning uchun `tasks.assigned_to` o'sha uid'da qolib ketardi. Endi: vazifalar **serverdan** o'qiladi (tasksCache to'liq emas), 0 bo'lsa oddiy `uiConfirm`, aks holda `rmxOpenModal` — ro'yxat + har qatorga tanlagich + "Hammasini bittaga" + 3 tugma (O'tkazib o'chirish / Biriktirmasdan o'chirish (`assigned_to=null`) / Bekor). `rmxApply` target bo'yicha guruhlab `.in()` bilan yangilaydi (`RMX_CHUNK=80`), `.select()` bilan qator sonini tekshiradi (RLS jimgina to'smasin), o'zgarishlar `thRecordMany` bilan BITTA insertda tarixga tushadi. Vazifa ko'chmasa xodim **o'chirilmaydi**. `rmxDoRemove` `.select()` qiladi — avval RLS to'sganda "O'chirildi" yolg'on chiqardi. CSS `.rmx-*`.
- **Vazifa tarixi (`th*`)**: `task_history` (TASKFIX_HISTORY.sql). Kuzatiladi FAQAT `deadline|assigned_to|status` (`TH_FIELDS`). `thBuildRows(prev,next)` → `thInsert(rows)`; ommaviy uchun `thRecordMany(pairs)` (bitta so'rov). Yagona choke-point `thRecord(prev, next)` — eski/yangi obyektni solishtiradi, `old_value/new_value` (xom) + `old_label/new_label` (o'qiladigan, `thLabel`) + `changed_by_name` yozadi. Chaqiriladi: `updateField`, `changeStatus`, `acceptTask`, `returnTask`, `tblUpdate` (nusxa optimistik yozuvdan OLDIN: `_thPrev`), `prjShiftNextDeadline`. Ko'rsatish: detal oynasida "Tarix" bo'limi (`thListHtml`, CSS `.th-*`), `openDetail` `Promise.all`da yuklaydi. Jadval yo'q bo'lsa `thTableMissing` aniqlaydi → `_thMissing=true`, xato faqat console'ga, saqlash oqimi buzilmaydi. ⚠️ Yozuv MIJOZDA — TG bot/EF to'g'ridan `tasks`ni yangilasa tarixga tushmaydi (trigger qo'shilmagan, ataylab).
- **Email bildirishnoma sozlamasi (`emailSet*`)**: `workspace_settings` jadvali (TASKFIX_EMAIL_SETTINGS.sql — **RUN kutilmoqda**). Har action alohida bayroq: `email_on_assign|acceptor|status|task_create|comment|deadline`. **Yagona choke-point `emailNotify(type,…)`** — `send-email` EF butun ilovada faqat o'sha yerdan chaqiriladi, `emailAllowed(type)` false bo'lsa **invoke umuman bo'lmaydi**. Turlar xaritasi `EMAIL_TYPE_KEY` (`task_assigned→assign`, `task_acceptor→acceptor`, `task_completed→status`). Yuklash: `loadEmailSettings()` — `loadCurrentContext` 1-to'lqinida, **hech qachon throw qilmaydi**; jadval yo'q → `_emailSetMissing=true` + `emailSetFallback()` = **hammasi yoniq** (eski xatti-harakat saqlanadi, bildirishnoma jimgina o'chmaydi). UI: Sozlamalar → "📧 Email bildirishnomalari" (`emailSetRender/Change/Save/Reset`, `EMAIL_SET_ROWS`, CSS `.esn-*`), faqat `isOwnerLike()`. `emailSetSave` **`.select()` qiladi** — RLS jimgina to'ssa "Saqlandi" yolg'on chiqmasin. SQL default'i **HAMMASI O'CHIQ (false)** — run'dan keyin hech qanday bildirishnoma emaili ketmaydi, admin UI'dan o'zi yoqadi. Skript oxirida `updated_by IS NULL` shartli tozalash UPDATE'i bor (UI'dan saqlangan tanlovni **qayta run** ham buzmaydi — `emailSetSave` doim `updated_by` yozadi). Mijozda **ikki xil zaxira**: `emailSetOff()` (hammasi false — jadval BOR, qator YO'Q holati, DB default bilan bir xil) va `emailSetFallback()` (hammasi true — faqat O'QIB BO'LMAGANDA: jadval yo'q/so'rov yiqildi, eski xatti-harakat saqlanadi). `task_create|comment|deadline` uchun mijozda yuboruvchi YO'Q (Telegram/ilova ichida bor) — UI'da "hozircha email yo'q" belgisi bilan ko'rsatiladi, bayroq `notifyN8n` payload'ida `email_settings` sifatida uzatiladi (n8n o'zi shoxlansin).
- **Email manbasi (`profiles.email`)** *(2026-08-08)*: ilova email'ni **FAQAT `public.profiles.email`** dan o'qiydi (`loadCurrentContext` → `profiles.select(... , email)`); `auth.users.email` mijozga faqat **o'zi uchun** (`me.email`) ochiq. Eski userlarda auth email profilga ko'chmagan → UI'da bo'sh ko'rinardi. 3 qatlam: (1) `TASKFIX_EMAIL_SYNC.sql` backfill; (2) o'sha fayldagi `zz_tf_auth_email_to_profile` trigger'i (`auth.users` INSERT/`UPDATE OF email` → `public.tf_set_profile_email(uuid,text)`; nomi ataylab `zz_` — mavjud `handle_new_user` DAN KEYIN ishlasin, aks holda uning INSERT'ini 23505 bilan yiqitardi; tanasi EXCEPTION→WARNING, auth oqimi hech qachon buzilmaydi; SECURITY DEFINER funksiya `anon`/`authenticated` dan REVOKE qilingan); (3) mijoz zaxirasi — `bootstrap()` dagi profil patch'i o'z qatorini to'ldiradi + `loadCurrentContext` da `profMap[me.id].email ||= me.email`. **Boshqa a'zoning** bo'sh emailini mijoz tuzata olmaydi (auth.users yopiq) — SQL shart. `admin-create-employee` EF ham endi `profiles.email` yozadi (deploy kutilmoqda). Mavjud email **hech qayerda bosilmaydi** (`admin-import-staff` "connect" bilan almashtirilgani saqlanadi).
- **Rasm**: `getPhotoUrl(uid)`/`empAvatarUrl`/`prefetchPhotoUrls` (signed URL kesh), `avatarHtml(name,size,url,uid)` — uid berilsa keshdan rasm. Yuklash: `phUploadPhoto` (canvas siqish).
- **Ish jadvali**: 3 rejim (weekly/monthly/flexible), `schOnModeChange`, `schSaveDays` (employee_schedule_days qayta hosil).
- **Routing**: `goPage`, `rtSetHash`/`rtRouteFromHash` (hash-routing, popstate).
- **Chap menu yig'ish (`sb*`)**: `sbInit()` (init'da), `sbToggle()` (brand-top dagi `#sbToggleBtn` + **Ctrl/Cmd+B**), `sbApply()`, `sbTitles()`. Holat `localStorage['sb_collapsed']='1'|'0'`; qiymat **YO'Q = foydalanuvchi tanlamagan** → ekran kengligiga qarab avtomatik (`SB_AUTO_W=1280`), bir marta bosgach tanlovi ustun (resize buzmaydi). `SB_MOBILE_W=880` dan tor ekranda qo'llanmaydi (sidebar u yerda gorizontal panel) — JS ham, media query'da zaxira override ham bor. CSS `.sb-collapsed` `#appView` da. ⚠️ Menyu matni alohida span'da EMAS (matn tuguni) → `.side-link{font-size:0}` + `.ic` da qaytariladi; matn hover'da `title` orqali (`sbTitles`, faqat matn tugunlarini yig'adi). `renderSidebar()` oxirida `sbTitles()` chaqiriladi.
- **Qidiruvli tanlash (`ss*`)** *(2026-08-06)*: butun ilova uchun **yagona** tanlash komponenti — `uiForm` maydon turi `search` (bitta) yoki `msearch` (ko'p). API: `ssFieldHtml(id,{items,value,multi,placeholder,empty})` → tugma HTML; `ssValue(id)` → single `'x'` / multi `['x','y']`; `ssForget(ids)` modal yopilganda tozalaydi. `items:[{value,label,sub,uid,search}]` — `uid` berilsa avatar, `search` qo'shimcha matn (telefon/email). Eski `options:[{value,label}]` ham qabul qilinadi.
  - **Qidiruv**: label + sub + search bo'yicha; 3+ raqam kiritilsa **telefon** bo'yicha ham (ikki tomon raqamlargacha normallashtiriladi — probel/plyus/tire ahamiyatsiz).
  - **Xodim tanlash**: `ssMemberItems(list)` — avatar + lavozim (`memberPos`) + telefon/email qidiruvi, alifbo tartibida, o'zi "Men" deb ko'rsatiladi. Hamma joyda shu ishlatiladi (bir xil ko'rinish/xatti-harakat).
  - ⚠️ Panel `<body>` ning bevosita farzandi + `position:fixed` — `gs*` dagi bilan bir xil sabab. Bir vaqtda **bitta** panel (`#ssPanel` qayta ishlatiladi, DOM ko'paymaydi); ochilganda `body` oxiriga ko'chiriladi.
  - 🔴 **Z-INDEX QATLAM XARITASI** (2026-08-06 da bug tuzatildi — panel `200` da edi va modal ortida qolib ko'rinmasdi): `200` eski qatlamlar · `400` qidiruv modali · `8000` `.tsk-chip` · **`9000` `.ui-modal-bd`** (uiForm/uiConfirm, `backdrop-filter` bilan) · **`9100` `.ss-panel`** · `9400` perf paneli · **`9500` `.toast`**. Yangi qoplama qo'shsangiz shu xaritaga qarab tanlang. ⚠️ `body` da `transform`/`filter` YO'Q — shuning uchun `position:fixed` to'g'ri ishlaydi; kelajakda qo'shilsa panel buziladi.
  - **Joylashuv** (`ssPosition`, `SS_MAX_H/GAP/EDGE/ROW_H`): pastda joy yetmasa **tepaga** ochiladi; ikkala tomon ham tor bo'lsa kattarog'i tanlanadi va panel o'sha joyga **sig'diriladi** (ichida scroll); chap/o'ng chekkadan chiqmaydi. Balandlik **ochilganda bir marta** variantlar soniga qarab hisoblanadi — qidiruv paytida qayta hisoblansa panel "sakrardi". Scroll (`capture:true`) va resize'da qayta joylashadi.
  - Klaviatura: ↑/↓ + Enter + Esc. Enter `stopPropagation` qiladi — `uiForm`ning "Enter → submit" i ishlab ketmasin. Tashqariga bosish listener'i **bir marta** bog'lanadi (`_ssBound`).
  - CSS `.ss-*`.
- **HR Service (`hrsv*`)** *(2026-08-06)*: "Qo'shimcha service" sahifasidagi ikkinchi karta (Telegram yonida). Holat `public.hr_settings` da — **`integrations` jadvaliga QO'SHILMADI** (u yerdagi `tool` CHECK'ini o'zgartirish mavjud Telegram/n8n integratsiyasiga xavf tug'dirardi; HR tashqi xizmat emas, ichki modul). `loadHrSvcSettings()` — `loadCurrentContext` 1-to'lqinida, **hech qachon throw qilmaydi**. Jadval yo'q → `_hrsvMissing=true` va **HR O'CHIQ** deb hisoblanadi (email sozlamasidan farqli: yangi modul jimgina yoqilib ketmasin, aks holda ishlamaydigan menyu chiqardi). `hrsvSetEnabled` upsert + `.select()`. Karta 4 holat ko'rsatadi: Sozlanmagan (SQL yo'q) / O'qilmadi / Yoqilgan / Yoqilmagan. Yoqilsa chap menyuda **HR** guruhi (Telegram naqshi: `side-group`+`side-sub`, `toggleHrSidebar`, localStorage `sb_hr_v1`, default YOPIQ) → "Org Schema".
- **Org Schema (`osc*`)** *(2026-08-06)*: `page-orgschema`, ikki ko'rinish bitta manbadan (`org_folders` + `org_people`, TASKFIX_HR.sql).
  - **Papka daraxti**: papkalarni foydalanuvchi **noldan o'zi yaratadi**. ⚠️ *2026-08-06 qarori*: TaskFix'dagi mavjud `departments` Org Schema'ga **AVTOMAT TORTILMAYDI** — `org_sync_departments()` RPC ham, `org_dept_rename_sync` trigger'i ham olib tashlandi (SQL'da `DROP IF EXISTS` bilan tozalanadi, tekshiruv bloki ular YO'Qligini tasdiqlaydi). Natijada TASKFIX_HR.sql `departments` jadvaliga **umuman tegmaydi** (FK ham qolmadi). Har qanday papkani o'chirish mumkin. `org_folders.kind`/`department_id` **ustunlari olib tashlandi** (SQL avval avtomat yaratilgan `kind='department'` papkalarni DELETE qiladi, keyin ustunlarni DROP qiladi — tekshiruv bloki ular yo'qligini tasdiqlaydi).
  - **🔴 BITTA BIRLASHGAN DARAXT (`oscTree()`)**: tugun papka ham, hodim ham bo'ladi va **har qanday tugun pastga davom etadi**. Ota polimorf, lekin **haqiqiy FK bilan** (ustun juftligi, `parent_kind text` EMAS — u FK yozishga imkon bermasdi va o'chgan tugun "osilib" qolardi):
    - `org_folders`: `parent_id` (papka ostida) **yoki** `parent_person_id` (hodim ostida)
    - `org_people`: `folder_id` (papkada) **yoki** `parent_person_id` (hodim ostida)
    - `CHECK` ikkovi bir vaqtda to'lmasin; `ON DELETE SET NULL` (CASCADE emas) — rahbar olib tashlansa uning ostidagi shox **o'chib ketmaydi**, ildizga ko'chadi.
    Tugun kaliti `oscKey(type,id)` → `'f:<id>'` / `'p:<id>'` (ikki jadval id'lari to'qnashmasin). Yordamchi: `oscSubtreeKeys` (avlod, sikl himoyali), `oscNodeOptions` (ota tanlash ro'yxati), `oscParentPatch`/`oscPersonParentPatch`.
  - **🔴 BO'YSUNISH — daraxtdan hisoblanadi, saqlanmaydi** (`manager_id` YO'Q, qo'lda tanlash yo'q): otasi **hodim** bo'lsa → o'sha hodim; otasi **papka** bo'lsa → o'sha papkadagilar **tengdosh** (o'zi ham shu yerda!), shuning uchun papka **o'tkazib yuboriladi** va yuqoridagi eng yaqin hodim(lar) rahbar bo'ladi; bo'sh papkalar ham o'tkaziladi; boshqa ildiz bilan bog'lanish yo'q. `oscDerivedManagers(person, tree)` → `oscMgrBadge`. ⚠️ "O'z papkasini o'tkazib yuborish" qadami MUHIM — busiz hodim o'zini va tengdoshlarini rahbar deb ko'rsatardi (2026-08-06 da aynan shu xato test bilan tutildi).
  - **Sikl**: daraxt ikki jadval bo'ylab yoyilgani uchun sikl **ARALASH** bo'lishi mumkin (papka→hodim→papka). Server tomonda yagona `org_check_ancestor(kind,id,pkind,pid,ws)` funksiyasi ikkala trigger'dan (`org_folders_guard_trg`, `org_people_guard_trg`) chaqiriladi. SQL tekshiruv bloki sof va aralash siklni **tirik** sinaydi.
  - **Tugun qatori** (`oscNodeRowHtml`, papka ham hodim ham): butun qator bosiladi → **faqat shu tugun** ochiladi/yopiladi (accordion EMAS). Chevron — bezak; amal tugmalari `event.stopPropagation()`. Klaviatura: `role="button"`+`tabindex`+`oscRowKey`. Har tugunda "+ hodim" va "+ papka".
  - **🔴 VIZUAL BIRLASHTIRISH (org chart)**: papkada **atigi bitta** hodim bo'lsa, papka va hodim ikki daraja emas, **bitta kartochka** bo'lib chiziladi ("CEO · Feruzbek"), bolalari = papkaning qolgan bolalari + o'sha hodimning bolalari. 2+ hodim bo'lsa eskicha (papka tuguni + ichida hodimlar). ⚠️ Bu **faqat chizish** — DB ham, `oscTree()` ham o'zgarmaydi; tahrir tugmalari ikkovini alohida boshqaradi (`type:'merged'`, CSS `.osc-card.merged`).
  - **Org chart** (`oscChartRoots`/`oscLayout`/`oscElbow`): `oscTree()` dan o'qiydi. ⚠️ Joylashuv har safar qaytadan hisoblanadi, **`node_x/node_y` SAQLANMAYDI** — ikki ko'rinish ajralib qola olmaydi (eski `oc*` dagi "qo'lda surilgan tugun eskiradi" muammosi yo'q). Algoritm: barglar qat'iy ustun egallaydi, ota bolalari ustida markazlashadi → ustma-ust tushish mumkin emas. Zoom/pan: `oscZoomFit/Set/By`, `oscBindPan` — window listener'lari **bir marta** (`_oscPanBound`).
  - Yozuv amallari faqat `isOwnerLike()`; **ko'rish har a'zoga ochiq** (RLS ham shunday) — shu sababli `orgschema` `managerOnlyPages` da EMAS, o'rniga `goPage` da `hrsvEnabled()` tekshiruvi.
  - Sikl himoyasi ikki qatlamda: server trigger `org_folders_guard_trg` (yagona haqiqiy himoya) + mijozda `seen` to'plami (buzilgan ma'lumot sahifani yiqitmasin). ⚠️ `org_people_guard` **olib tashlandi** — bo'ysunish saqlanmagach kerak emas.
  - CSS `.osc-*` (810–894-qatorlar, moslashtirish blokidan OLDIN — qoida saqlangan).
- **⚠️ Eski "Tashkilot tuzilmasi" (`oc*` / `org_containers`)** — *2026-08-06 dan vaqtincha YASHIRILGAN*, o'chirilmagan. Jamoa sahifasidagi **butun tab qatori** (`view-tabs`: "👥 Hodimlar" + "🏢 Tashkilot") HTML izohiga olindi — Tashkilot HR'ga ko'chgach bitta tab qolib ma'nosiz bo'lgandi; `teamPaneHodimlar` endi to'g'ridan-to'g'ri ko'rinadi. `gsPushPages` dagi "Tashkilot tuzilmasi" yozuvi ham izohda. `teamSetTab()` saqlanmoqda (`goPage('team')` uni 'hodimlar' bilan chaqiradi). Kod ham, DB ma'lumoti ham joyida; qaytarish uchun izohni ochish yetarli.
- **Ikonkalar (`ic*`)**: `ICON_PATHS` (23 ta, Lucide geometriyasi, 24×24) — 2026-08-06 da qo'shildi: `hr` (contact-round), `network`, `folderOpen`, `trash`, `pencil`, `userPlus`. + `icon(name,size)` + `sideIcon(name)`. **Inline SVG, tashqi CDN YO'Q** (build yo'q + CDN beqaror). Noma'lum nom → `console.warn` + bo'sh matn. Ishlatilgan: chap menu, `add-mini` (+), `msFilter` qatorlari, sidebar toggle chevron (CSS bilan aylanadi). Sahifa sarlavhalarida emoji **yo'q** — ikonkani chap menu ko'taradi.
- **Responsive (kichik ekranli kompyuter)**: ⚠️ ASOSIY — `.main { min-width: 0 }`. `.app.show` grid'ining `1fr` track'i sukut bo'yicha `min-width:auto`, ya'ni kontentdan kichik bo'lolmaydi → kanban/keng jadval **butun sahifani** gorizontal scroll qilardi (1024px: scrollWidth 1752 / clientWidth 1014). Endi keng kontent o'z idishida scroll bo'ladi (`.kanban`/`.tbl-wrap`/`.pln-cal`). Moslashtirish bloki **stylesheet OXIRIDA** (media query specificity qo'shmaydi → `.kan-col`/`.pln-grid` dan keyin turishi SHART) va `min-width:981px` chegarali (980/880px dagi mobil qoidalar buzilmasin): ≤1440 padding, 981–1280 (kan-col 252, pln-grid 48+7×96, dashSplit 1 ustun), 981–1180 (kan-col 236), ≤1180 pln-layout 1 ustun, `max-height:780px` balandlik tuzatishlari.
- **Universal qidiruv (`gs*`)**: `// ============ 🔍 UNIVERSAL QIDIRUV (gs*) ============` bloki, `init()`dan **oldin, global scope'da** (~14222). **Modal YO'Q** — topbardagi `#topSearchInp` haqiqiy input (`oninput=gsOnInput`, `onkeydown=gsKey`, `onfocus=gsOpen`), natijalar `#gsDrop`/`#gsList` panelida uning ostida ochiladi (`.gs-drop`, `@keyframes gsDrop` .22s). Panel **`<body>`ning bevosita farzandi** + `gsPosition()` (fixed, `getBoundingClientRect`) — topbar `sticky`/`z-index:20` bilan urishmasin; `z-index:90` (modal 100 ostida). ⌘K → `gsFocus()` (fokus + select + ochish). `gsClose()` matnni saqlaydi (Esc/tashqariga bosish, `gsWatch` mousedown), `gsDone()` natija tanlangandan keyin tozalaydi. Xodim qatorida **`＋ Vazifa`** tugmasi (`.gs-act` → `gsNewTaskFor(uid)`): `openCreate()` + `msSelected=[{type:'user'|'me',id,label}]` + `msUpdateSummary()`/`msFilter()`. Shu sababli natija elementi `<div role="option">` (button ichida button bo'lmasligi uchun). Yagona dvigatel: `gsLocal(q,qd)` (kesh: wsMembers/tasksCache/departments/branchesCache/positionsCache/projectsCache) + `gsServer(q)` (parallel `ilike`: tasks/projects/positions, har biri `{error}`) → `gsApplyServer` (id bo'yicha dublikatsiz) → `gsPaint()`. Guruhlar `GS_GROUPS`, guruhda 5 ta + `gsShowMore`. Telefon `gsPhoneMatch`/`gsHiPhone` (raqamlargacha normallashtirish, oxirgi 9 raqam). Highlight `gsHi`/`gsHiPhone` (escapeHtml ichida). Debounce `GS_DEBOUNCE`, eskirgan javob `_gsSeq` bilan bekor. CSS `.gs-*`; mobilda (`@media max-width:640px`) panel deyarli butun kenglik (`gsPosition` `innerWidth<=640`). `openCmdPalette`/`closeCmdPalette` — eski nom aliaslari (⌘K listener va init'dagi Escape ishlatadi; Escape `_gsShown`ga qaraydi). Tezlik uchun ixtiyoriy `TASKFIX_SEARCH.sql` (pg_trgm GIN).
  - ⚠️ **Saboq (2026-07-28)**: eski qidiruv butunlay `init()` ICHIDA edi → funksiyalar lokal, HTML inline handler esa global scope'da baholanadi → `openCmdPalette is not defined`. **Inline handler chaqiradigan funksiya global bo'lishi SHART** (top-level `function`). Top-level `const`/`let` (masalan `$`) inline handler'ga ko'rinadi, funksiya ichidagilar YO'Q.
- **Import**: `hrImport*` (preflight 5b — telefon+ism blokeri mijozda).
- **EF**: `admin-import-staff` (phase: identity | photos | connect), `sync-provodka-kassa` (Harajat kassa → Provodka RPC), `admin-create-employee` (v4 — xodim yaratish/topish + a'zolik + lavozim/filial + parol emaili; **repoda tiklandi 2026-07-23**, deploy kutilmoqda). Boshqa EF manbalari repoda YO'Q (send-email, tg-send, tg-webhook... deployed).
- **Loyiha (Project)**: `// ============ LOYIHALAR ============` bloki (~2620–3000) + flow yordamchilari (`prjShiftNextDeadline`/`prjReloadTasks`). Turi `projects.flow_type` (`sequential`|`parallel`, default sequential) — **faqat yangi bosqich uchun default** (`prjAddTask` toggle'i), har vazifa o'z `depends_on_prev`ini saqlaydi. Qulf: `tasks.is_locked` (server trigger **avtoritet**) — `prjTaskOpen` avval `is_locked`ka qaraydi, `prjShouldBeOpen` faqat mantiqni hisoblaydi (ikkisi ziddiyatda = "qulf qotib qolgan" → owner'ga `prjUnlockTask` tugmasi). Boshqa: `prjRenderHero` (kim ushlab turibdi), `prjMoveTask` (↑↓ tartib), `prjSyncLocks`, `prjApplyTypeToTasks`, `prjRenderHeader`, `prjShowError`. CSS `.prj-*`.
  - **Refresh qoidasi**: vazifani o'zgartiruvchi HAR amal `tasksCache` VA `prjTasks` ni yangilashi shart (`refreshActiveView` faqat tasksCache'dan sinxronlaydi — o'chgan qator u yerda yo'q, shuning uchun o'zi tozalanmaydi). O'chirish/ko'chirishdan keyin `loadProjectDetail()` yoki `prjReloadTasks()` (u endi serverda yo'q bosqichlarni ham tozalaydi). Mavjud bo'lmagan vazifa uchun `taskGoneLocal(id)` — ro'yxatni tozalaydi, xom "topilmadi" xatosi o'rniga yumshoq xabar; `isRowGone(err)` PGRST116'ni aniqlaydi.
- **Provodka integratsiyasi**: `hk*` funksiyalar (`hkSync`/`hkTableToggle`/`hkSetDb`). Jamoa jadval 💵 ustuni + hodim detali checkbox. EF `sync-provodka-kassa` env: `PROVODKA_URL`, `PROVODKA_SERVICE_KEY`.

## Ochiq masalalar
- **Dublikatlar**: Akobir tasdiqlangan (`cleanup_duplicate_staff.sql`da qo'lda juftlik). ~14 haqiqiy odam, qolgan 126 sintetik. Qo'shimcha juftliklar `dup_pairs` `manual` ro'yxatiga qo'lda qo'shiladi.
- **EF deploy kutilmoqda**: `admin-import-staff` v3.1 (connect action), `sync-provodka-kassa` v1 (+ 2 env secret: PROVODKA_URL, PROVODKA_SERVICE_KEY).
- **SQL kutilmoqda**: 43, 45, 47, `TASKFIX_V8.sql`, `TASKFIX_PROJECT.sql`, `TASKFIX_HISTORY.sql` (vazifa tarixi — busiz "Tarix" bo'limi "yoqilmagan" deb yozadi), `TASKFIX_SEARCH.sql` (ixtiyoriy — qidiruv indeksi, ilova busiz ham ishlaydi), `TASKFIX_EMAIL_SETTINGS.sql` (email toggle'lari — busiz ilova ishlaydi, lekin sozlama saqlanmaydi va hamma email yoniq qoladi), **`TASKFIX_HR.sql`** (HR Service + Org Schema: `hr_settings`, `org_folders`, `org_people` + sikl triggerlari; `departments` ga TEGMAYDI — **busiz HR Service yoqilmaydi**, karta buni ochiq aytadi, qolgan ilova ishlayveradi), **`TASKFIX_EMAIL_SYNC.sql`** (profiles.email backfill + auth trigger — busiz eski xodimlarning emaili bo'sh ko'rinaveradi, faqat o'zi kirgan foydalanuvchi mijoz zaxirasi bilan tuzaladi) (TaskFix); `PROVODKA_HODIM_KASSA.sql` (Provodka loyihasida).
- **Scale (2026-07-30, BRIEF_TASKFIX_SCALE.md)**: `TASKFIX_SCALE.sql` — **RUN kutilmoqda**. Bitta tranzaksiyada: (a) indekslar `idx_tasks_ws_created(ws,created_at DESC,id DESC)`, `idx_tasks_ws_status`, `idx_tasks_ws_deadline`, `idx_ws_members_user_ws`; (b) RLS: `pg_policies` dan o'qib `auth.uid()|jwt()|role()` → `(SELECT ...)` bilan o'rab `ALTER POLICY`. **Xavfsizlik**: o'zgarishdan oldin/keyin har foydalanuvchi × jadval bo'yicha ko'rinadigan qator sanaladi (`authenticated` roli ostida); bitta raqam farq qilsa `RAISE EXCEPTION` → DDL ham qaytadi. Asl policy matni `public.taskfix_rls_backup` da, qaytarish `TASKFIX_SCALE_ROLLBACK.sql`. Yakka `tasks(status)`/`tasks(deadline)` indekslari **ataylab qo'shilmadi** (so'rovlar doim `workspace_id` bilan keladi → kompozit yaxshiroq; `v_add_singles` bilan yoqiladi). Skript o'zi before/after `EXPLAIN ANALYZE` jadvalini chiqaradi.
- **Stress/perf test (2026-07-30, BRIEF_TASKFIX_STRESS.md)**: `STRESS_TEST_README.md` (tartib, natija, tuzatishlar). **1-test BAJARILDI**: boot 6079ms / 9 to'lqin / 2.1× — sekinlik ketma-ketlikda, DB'da emas (har so'rov 300–400ms). **Boot parallellashtirildi** (pastga qarang) → kutilgan 4 to'lqin. **2 va 3-test RUN kutilmoqda**: `stress_test.js` (10/50/100/500, faqat GET, `--yes` shart, xato>30% da bekor), `TASKFIX_STRESS_VOLUME.sql` + `TASKFIX_STRESS_CLEANUP.sql` (1k/10k/50k, alohida test ws `a57e5511-…`, Aros qora ro'yxatda). Hali ochiq: **`loadTasksFull()` LIMITSIZ** (owner uchun butun ws vazifalari) — indeks bilan hal bo'lmaydi, pagination kerak; 3-test shuni o'lchaydi.
- **Vazifalarni sahifalab yuklash (2026-07-30)**: `loadTasksFull()` endi LIMITSIZ emas. `TASKS_PAGE=500` — 1-sahifa darrov, qolgani **fonda** `tasksLoadRest()` bilan (`await` YO'Q), har sahifadan keyin `refreshActiveView()`. UX o'zgarmadi (foydalanuvchi baribir hammasini ko'radi), faqat 1-ekran tez. `tasksQuery(from,to)` — owner/a'zo farqi shu yerda. ⚠️ **ANCHOR**: 2-sahifadan boshlab `created_at <= _tasksAnchor` (1-sahifadagi eng yangi qator vaqti, SERVER qiymati) — busiz yuklash paytida yangi vazifa qo'shilsa offset siljib qator tushib qolardi. `_tasksOffset` alohida hisoblanadi (`tasksCache.length` EMAS — realtime uni siljitadi), qo'shishda id bo'yicha dublikat tekshiruvi. `TASKS_MAX=20000` chegarasi — yetilsa `console.error` + toast (jimgina kesilmaydi). Tartib `created_at DESC, id DESC` — `idx_tasks_ws_created` shuni qoplaydi. CSS `.tsk-chip`.
- **Boot yo'li (2026-07-30 optimizatsiya)**: `bootstrap()` — `auth.getSession()` (avval `getUser()` edi: har boot'da tarmoq; getSession localStorage'dan, token eskirgandagina tarmoqqa chiqadi) → `profiles` **∥** `loadWorkspaces()` (`Promise.all`). `loadCurrentContext()` — **2 to'lqin**: (1) 9 parallel (ws so'rovlari + `loadKanbanColumns` + `tg_bot_users(linked_user_id)`), (2) `profiles(a'zolar)` ∥ `tg_bot_users(integration)` ∥ `loadTasksFull()`. `loadHrData()` **fonda** (`.then` bilan, await YO'Q) — lavozim/rasm bezak, ilova ularni kutmaydi; tayyor bo'lgach `refreshActiveView`/`renderTeam`/`msFilter` jimgina yangilanadi. ⚠️ Fon bo'lgani uchun `loadHrData` ichida "ws almashdimi" tekshiruvi bor (eski natija yangisini bosmasin). `_myTgBotIds` 1-to'lqinda bir marta o'qiladi, `loadTasksFull` keshdan oladi (avval ikki marta so'ralardi).
- **Dublikat birlashtirish (2026-07-29, BRIEF_TASKFIX_MERGE.md + _2)**: `TASKFIX_MERGE_DIAG.sql` (1a — **faqat SELECT**) bajarildi. `TASKFIX_MERGE_1B.sql` (1b — **YOZADI**, RUN kutilmoqda): 2 juftlik qattiq yozilgan — Nodir KEEP `d7d8f5f3…` / REM `eb0d0a9e…` (6 vazifa kutiladi), Saidakbar KEEP `840a22ea…` / REM `50ef8131…` (12). Havolalar **dinamik** ko'chiriladi: merge paytida public+storage'dagi har uuid ustuni skanerlanadi (qo'lda ro'yxat YO'Q), UNIQUE indeks bo'yicha to'qnashadigan REMOVE qatorlari oldin o'chiriladi, `employee_details` esa maydon-maydon birlashtiriladi. Bitta tranzaksiya + oxirida RAISE tekshiruv (KEEP bor/REMOVE yo'q/havola qolmagan/vazifa soni). `auth.users` o'chirish faqat login qilmaganda va ichki EXCEPTION blokida (merge'ni qaytarmaydi). Rasm (Storage) SQL bilan ko'chmaydi — WARNING beradi. Universal emas: yangi dublikat chiqsa yangi diagnostika + yangi fayl.
- **Loyiha moduli (2026-07-27)**: (1) `TASKFIX_PROJECT.sql` — `projects.flow_type` + diagnostika (status CHECK, RLS, flow ustunlari). Ustun bo'lmasa ilova ishlaydi: `prjNoFlowTypeCol(err)` aniqlaydi, flow_type'siz qayta yozadi va ogohlantiradi. (2) `projects.status` — UI `active|paused|done` yozadi ("Sozlash"), DB'da CHECK bo'lsa xato chiqishi mumkin (SQL diagnostikasi shuni tekshiradi). (3) **Takrorlanish hamon LAZY** — reset faqat owner/admin loyihani ochganda (`prjRecurDue`+`prjResetRecurrence`); cron yo'q. (4) Loyiha RLS'i (34-migratsiya) repoda yo'q — server tomon tekshirilmagan, UI'da hamma o'zgartiruvchi amal `isOwnerLike()` bilan yopilgan.
- **V8 (2026-07-24)**: (1) "Bajardi: {ism} · sana" vazifa detalida — `tasks.bajardi_user_id`/`bajardi_at` (TASKFIX_V8.sql). `changeStatus` best-effort yozadi (ustunlar bo'lmasa jimgina console.error, zaxira `submitter_id`/`completed_at`). (2) Xodim filtri — `empFilter` (Set), `applyEmpFilter` (`tasks` + `department` sahifalari), yagona global filtr 2 bar bilan: 'tasks' (`#empFilterPanel`) va 'dept' (`#deptEmpFilterPanel`). localStorage `empFilter_<ws>`. Tasks: list/kanban/kalendar/jadval; Bo'lim detali: kanban/list/kalendar (Jamoa tabida bar yashirin). Planner'ga qo'shilmagan (yashirin filtrlanish oldini olish uchun `applyEmpFilter` faqat shu 2 sahifada). Funksiyalar scope-aware: `empFilterTogglePanel/RenderList(scope)`.

## Validatsiya buyrug'i
```bash
node -e "
const fs=require('fs'), vm=require('vm');
const h=fs.readFileSync('index.html','utf8');
const re=/<script(?![^>]*src=)[^>]*>([\s\S]*?)<\/script>/g;
let m, code=null;
while((m=re.exec(h))){ if(m[1].indexOf('async function init')!==-1) code=m[1]; }
try { new vm.Script(code); console.log('OK', code.length); }
catch(e){ console.log('XATO:', e.message); process.exit(1); }
"
```
